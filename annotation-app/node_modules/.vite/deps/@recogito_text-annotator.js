// node_modules/uuid/dist/regex.js
var regex_default = /^(?:[0-9a-f]{8}-[0-9a-f]{4}-[1-8][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}|00000000-0000-0000-0000-000000000000|ffffffff-ffff-ffff-ffff-ffffffffffff)$/i;

// node_modules/uuid/dist/validate.js
function validate(uuid) {
  return typeof uuid === "string" && regex_default.test(uuid);
}
var validate_default = validate;

// node_modules/uuid/dist/parse.js
function parse(uuid) {
  if (!validate_default(uuid)) {
    throw TypeError("Invalid UUID");
  }
  let v2;
  return Uint8Array.of((v2 = parseInt(uuid.slice(0, 8), 16)) >>> 24, v2 >>> 16 & 255, v2 >>> 8 & 255, v2 & 255, (v2 = parseInt(uuid.slice(9, 13), 16)) >>> 8, v2 & 255, (v2 = parseInt(uuid.slice(14, 18), 16)) >>> 8, v2 & 255, (v2 = parseInt(uuid.slice(19, 23), 16)) >>> 8, v2 & 255, (v2 = parseInt(uuid.slice(24, 36), 16)) / 1099511627776 & 255, v2 / 4294967296 & 255, v2 >>> 24 & 255, v2 >>> 16 & 255, v2 >>> 8 & 255, v2 & 255);
}
var parse_default = parse;

// node_modules/uuid/dist/stringify.js
var byteToHex = [];
for (let i2 = 0; i2 < 256; ++i2) {
  byteToHex.push((i2 + 256).toString(16).slice(1));
}
function unsafeStringify(arr, offset = 0) {
  return (byteToHex[arr[offset + 0]] + byteToHex[arr[offset + 1]] + byteToHex[arr[offset + 2]] + byteToHex[arr[offset + 3]] + "-" + byteToHex[arr[offset + 4]] + byteToHex[arr[offset + 5]] + "-" + byteToHex[arr[offset + 6]] + byteToHex[arr[offset + 7]] + "-" + byteToHex[arr[offset + 8]] + byteToHex[arr[offset + 9]] + "-" + byteToHex[arr[offset + 10]] + byteToHex[arr[offset + 11]] + byteToHex[arr[offset + 12]] + byteToHex[arr[offset + 13]] + byteToHex[arr[offset + 14]] + byteToHex[arr[offset + 15]]).toLowerCase();
}

// node_modules/uuid/dist/rng.js
var rnds8 = new Uint8Array(16);
function rng() {
  return crypto.getRandomValues(rnds8);
}

// node_modules/uuid/dist/md5.js
function md5(bytes) {
  const words = uint8ToUint32(bytes);
  const md5Bytes = wordsToMd5(words, bytes.length * 8);
  return uint32ToUint8(md5Bytes);
}
function uint32ToUint8(input) {
  const bytes = new Uint8Array(input.length * 4);
  for (let i2 = 0; i2 < input.length * 4; i2++) {
    bytes[i2] = input[i2 >> 2] >>> i2 % 4 * 8 & 255;
  }
  return bytes;
}
function getOutputLength(inputLength8) {
  return (inputLength8 + 64 >>> 9 << 4) + 14 + 1;
}
function wordsToMd5(x2, len) {
  const xpad = new Uint32Array(getOutputLength(len)).fill(0);
  xpad.set(x2);
  xpad[len >> 5] |= 128 << len % 32;
  xpad[xpad.length - 1] = len;
  x2 = xpad;
  let a3 = 1732584193;
  let b2 = -271733879;
  let c2 = -1732584194;
  let d2 = 271733878;
  for (let i2 = 0; i2 < x2.length; i2 += 16) {
    const olda = a3;
    const oldb = b2;
    const oldc = c2;
    const oldd = d2;
    a3 = md5ff(a3, b2, c2, d2, x2[i2], 7, -680876936);
    d2 = md5ff(d2, a3, b2, c2, x2[i2 + 1], 12, -389564586);
    c2 = md5ff(c2, d2, a3, b2, x2[i2 + 2], 17, 606105819);
    b2 = md5ff(b2, c2, d2, a3, x2[i2 + 3], 22, -1044525330);
    a3 = md5ff(a3, b2, c2, d2, x2[i2 + 4], 7, -176418897);
    d2 = md5ff(d2, a3, b2, c2, x2[i2 + 5], 12, 1200080426);
    c2 = md5ff(c2, d2, a3, b2, x2[i2 + 6], 17, -1473231341);
    b2 = md5ff(b2, c2, d2, a3, x2[i2 + 7], 22, -45705983);
    a3 = md5ff(a3, b2, c2, d2, x2[i2 + 8], 7, 1770035416);
    d2 = md5ff(d2, a3, b2, c2, x2[i2 + 9], 12, -1958414417);
    c2 = md5ff(c2, d2, a3, b2, x2[i2 + 10], 17, -42063);
    b2 = md5ff(b2, c2, d2, a3, x2[i2 + 11], 22, -1990404162);
    a3 = md5ff(a3, b2, c2, d2, x2[i2 + 12], 7, 1804603682);
    d2 = md5ff(d2, a3, b2, c2, x2[i2 + 13], 12, -40341101);
    c2 = md5ff(c2, d2, a3, b2, x2[i2 + 14], 17, -1502002290);
    b2 = md5ff(b2, c2, d2, a3, x2[i2 + 15], 22, 1236535329);
    a3 = md5gg(a3, b2, c2, d2, x2[i2 + 1], 5, -165796510);
    d2 = md5gg(d2, a3, b2, c2, x2[i2 + 6], 9, -1069501632);
    c2 = md5gg(c2, d2, a3, b2, x2[i2 + 11], 14, 643717713);
    b2 = md5gg(b2, c2, d2, a3, x2[i2], 20, -373897302);
    a3 = md5gg(a3, b2, c2, d2, x2[i2 + 5], 5, -701558691);
    d2 = md5gg(d2, a3, b2, c2, x2[i2 + 10], 9, 38016083);
    c2 = md5gg(c2, d2, a3, b2, x2[i2 + 15], 14, -660478335);
    b2 = md5gg(b2, c2, d2, a3, x2[i2 + 4], 20, -405537848);
    a3 = md5gg(a3, b2, c2, d2, x2[i2 + 9], 5, 568446438);
    d2 = md5gg(d2, a3, b2, c2, x2[i2 + 14], 9, -1019803690);
    c2 = md5gg(c2, d2, a3, b2, x2[i2 + 3], 14, -187363961);
    b2 = md5gg(b2, c2, d2, a3, x2[i2 + 8], 20, 1163531501);
    a3 = md5gg(a3, b2, c2, d2, x2[i2 + 13], 5, -1444681467);
    d2 = md5gg(d2, a3, b2, c2, x2[i2 + 2], 9, -51403784);
    c2 = md5gg(c2, d2, a3, b2, x2[i2 + 7], 14, 1735328473);
    b2 = md5gg(b2, c2, d2, a3, x2[i2 + 12], 20, -1926607734);
    a3 = md5hh(a3, b2, c2, d2, x2[i2 + 5], 4, -378558);
    d2 = md5hh(d2, a3, b2, c2, x2[i2 + 8], 11, -2022574463);
    c2 = md5hh(c2, d2, a3, b2, x2[i2 + 11], 16, 1839030562);
    b2 = md5hh(b2, c2, d2, a3, x2[i2 + 14], 23, -35309556);
    a3 = md5hh(a3, b2, c2, d2, x2[i2 + 1], 4, -1530992060);
    d2 = md5hh(d2, a3, b2, c2, x2[i2 + 4], 11, 1272893353);
    c2 = md5hh(c2, d2, a3, b2, x2[i2 + 7], 16, -155497632);
    b2 = md5hh(b2, c2, d2, a3, x2[i2 + 10], 23, -1094730640);
    a3 = md5hh(a3, b2, c2, d2, x2[i2 + 13], 4, 681279174);
    d2 = md5hh(d2, a3, b2, c2, x2[i2], 11, -358537222);
    c2 = md5hh(c2, d2, a3, b2, x2[i2 + 3], 16, -722521979);
    b2 = md5hh(b2, c2, d2, a3, x2[i2 + 6], 23, 76029189);
    a3 = md5hh(a3, b2, c2, d2, x2[i2 + 9], 4, -640364487);
    d2 = md5hh(d2, a3, b2, c2, x2[i2 + 12], 11, -421815835);
    c2 = md5hh(c2, d2, a3, b2, x2[i2 + 15], 16, 530742520);
    b2 = md5hh(b2, c2, d2, a3, x2[i2 + 2], 23, -995338651);
    a3 = md5ii(a3, b2, c2, d2, x2[i2], 6, -198630844);
    d2 = md5ii(d2, a3, b2, c2, x2[i2 + 7], 10, 1126891415);
    c2 = md5ii(c2, d2, a3, b2, x2[i2 + 14], 15, -1416354905);
    b2 = md5ii(b2, c2, d2, a3, x2[i2 + 5], 21, -57434055);
    a3 = md5ii(a3, b2, c2, d2, x2[i2 + 12], 6, 1700485571);
    d2 = md5ii(d2, a3, b2, c2, x2[i2 + 3], 10, -1894986606);
    c2 = md5ii(c2, d2, a3, b2, x2[i2 + 10], 15, -1051523);
    b2 = md5ii(b2, c2, d2, a3, x2[i2 + 1], 21, -2054922799);
    a3 = md5ii(a3, b2, c2, d2, x2[i2 + 8], 6, 1873313359);
    d2 = md5ii(d2, a3, b2, c2, x2[i2 + 15], 10, -30611744);
    c2 = md5ii(c2, d2, a3, b2, x2[i2 + 6], 15, -1560198380);
    b2 = md5ii(b2, c2, d2, a3, x2[i2 + 13], 21, 1309151649);
    a3 = md5ii(a3, b2, c2, d2, x2[i2 + 4], 6, -145523070);
    d2 = md5ii(d2, a3, b2, c2, x2[i2 + 11], 10, -1120210379);
    c2 = md5ii(c2, d2, a3, b2, x2[i2 + 2], 15, 718787259);
    b2 = md5ii(b2, c2, d2, a3, x2[i2 + 9], 21, -343485551);
    a3 = safeAdd(a3, olda);
    b2 = safeAdd(b2, oldb);
    c2 = safeAdd(c2, oldc);
    d2 = safeAdd(d2, oldd);
  }
  return Uint32Array.of(a3, b2, c2, d2);
}
function uint8ToUint32(input) {
  if (input.length === 0) {
    return new Uint32Array();
  }
  const output = new Uint32Array(getOutputLength(input.length * 8)).fill(0);
  for (let i2 = 0; i2 < input.length; i2++) {
    output[i2 >> 2] |= (input[i2] & 255) << i2 % 4 * 8;
  }
  return output;
}
function safeAdd(x2, y3) {
  const lsw = (x2 & 65535) + (y3 & 65535);
  const msw = (x2 >> 16) + (y3 >> 16) + (lsw >> 16);
  return msw << 16 | lsw & 65535;
}
function bitRotateLeft(num, cnt) {
  return num << cnt | num >>> 32 - cnt;
}
function md5cmn(q2, a3, b2, x2, s2, t2) {
  return safeAdd(bitRotateLeft(safeAdd(safeAdd(a3, q2), safeAdd(x2, t2)), s2), b2);
}
function md5ff(a3, b2, c2, d2, x2, s2, t2) {
  return md5cmn(b2 & c2 | ~b2 & d2, a3, b2, x2, s2, t2);
}
function md5gg(a3, b2, c2, d2, x2, s2, t2) {
  return md5cmn(b2 & d2 | c2 & ~d2, a3, b2, x2, s2, t2);
}
function md5hh(a3, b2, c2, d2, x2, s2, t2) {
  return md5cmn(b2 ^ c2 ^ d2, a3, b2, x2, s2, t2);
}
function md5ii(a3, b2, c2, d2, x2, s2, t2) {
  return md5cmn(c2 ^ (b2 | ~d2), a3, b2, x2, s2, t2);
}
var md5_default = md5;

// node_modules/uuid/dist/v35.js
function stringToBytes(str) {
  str = unescape(encodeURIComponent(str));
  const bytes = new Uint8Array(str.length);
  for (let i2 = 0; i2 < str.length; ++i2) {
    bytes[i2] = str.charCodeAt(i2);
  }
  return bytes;
}
var DNS = "6ba7b810-9dad-11d1-80b4-00c04fd430c8";
var URL = "6ba7b811-9dad-11d1-80b4-00c04fd430c8";
function v35(version, hash, value, namespace, buf, offset) {
  const valueBytes = typeof value === "string" ? stringToBytes(value) : value;
  const namespaceBytes = typeof namespace === "string" ? parse_default(namespace) : namespace;
  if (typeof namespace === "string") {
    namespace = parse_default(namespace);
  }
  if (namespace?.length !== 16) {
    throw TypeError("Namespace must be array-like (16 iterable integer values, 0-255)");
  }
  let bytes = new Uint8Array(16 + valueBytes.length);
  bytes.set(namespaceBytes);
  bytes.set(valueBytes, namespaceBytes.length);
  bytes = hash(bytes);
  bytes[6] = bytes[6] & 15 | version;
  bytes[8] = bytes[8] & 63 | 128;
  if (buf) {
    offset ??= 0;
    if (offset < 0 || offset + 16 > buf.length) {
      throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
    }
    for (let i2 = 0; i2 < 16; ++i2) {
      buf[offset + i2] = bytes[i2];
    }
    return buf;
  }
  return unsafeStringify(bytes);
}

// node_modules/uuid/dist/v3.js
function v3(value, namespace, buf, offset) {
  return v35(48, md5_default, value, namespace, buf, offset);
}
v3.DNS = DNS;
v3.URL = URL;

// node_modules/uuid/dist/v4.js
function v4(options, buf, offset) {
  if (!buf && !options && crypto.randomUUID) {
    return crypto.randomUUID();
  }
  return _v4(options, buf, offset);
}
function _v4(options, buf, offset) {
  options = options || {};
  const rnds = options.random ?? options.rng?.() ?? rng();
  if (rnds.length < 16) {
    throw new Error("Random bytes length must be >= 16");
  }
  rnds[6] = rnds[6] & 15 | 64;
  rnds[8] = rnds[8] & 63 | 128;
  if (buf) {
    offset = offset || 0;
    if (offset < 0 || offset + 16 > buf.length) {
      throw new RangeError(`UUID byte range ${offset}:${offset + 15} is out of buffer bounds`);
    }
    for (let i2 = 0; i2 < 16; ++i2) {
      buf[offset + i2] = rnds[i2];
    }
    return buf;
  }
  return unsafeStringify(rnds);
}
var v4_default = v4;

// node_modules/uuid/dist/sha1.js
function f(s2, x2, y3, z2) {
  switch (s2) {
    case 0:
      return x2 & y3 ^ ~x2 & z2;
    case 1:
      return x2 ^ y3 ^ z2;
    case 2:
      return x2 & y3 ^ x2 & z2 ^ y3 & z2;
    case 3:
      return x2 ^ y3 ^ z2;
  }
}
function ROTL(x2, n2) {
  return x2 << n2 | x2 >>> 32 - n2;
}
function sha1(bytes) {
  const K3 = [1518500249, 1859775393, 2400959708, 3395469782];
  const H2 = [1732584193, 4023233417, 2562383102, 271733878, 3285377520];
  const newBytes = new Uint8Array(bytes.length + 1);
  newBytes.set(bytes);
  newBytes[bytes.length] = 128;
  bytes = newBytes;
  const l2 = bytes.length / 4 + 2;
  const N3 = Math.ceil(l2 / 16);
  const M3 = new Array(N3);
  for (let i2 = 0; i2 < N3; ++i2) {
    const arr = new Uint32Array(16);
    for (let j2 = 0; j2 < 16; ++j2) {
      arr[j2] = bytes[i2 * 64 + j2 * 4] << 24 | bytes[i2 * 64 + j2 * 4 + 1] << 16 | bytes[i2 * 64 + j2 * 4 + 2] << 8 | bytes[i2 * 64 + j2 * 4 + 3];
    }
    M3[i2] = arr;
  }
  M3[N3 - 1][14] = (bytes.length - 1) * 8 / 2 ** 32;
  M3[N3 - 1][14] = Math.floor(M3[N3 - 1][14]);
  M3[N3 - 1][15] = (bytes.length - 1) * 8 & 4294967295;
  for (let i2 = 0; i2 < N3; ++i2) {
    const W2 = new Uint32Array(80);
    for (let t2 = 0; t2 < 16; ++t2) {
      W2[t2] = M3[i2][t2];
    }
    for (let t2 = 16; t2 < 80; ++t2) {
      W2[t2] = ROTL(W2[t2 - 3] ^ W2[t2 - 8] ^ W2[t2 - 14] ^ W2[t2 - 16], 1);
    }
    let a3 = H2[0];
    let b2 = H2[1];
    let c2 = H2[2];
    let d2 = H2[3];
    let e2 = H2[4];
    for (let t2 = 0; t2 < 80; ++t2) {
      const s2 = Math.floor(t2 / 20);
      const T = ROTL(a3, 5) + f(s2, b2, c2, d2) + e2 + K3[s2] + W2[t2] >>> 0;
      e2 = d2;
      d2 = c2;
      c2 = ROTL(b2, 30) >>> 0;
      b2 = a3;
      a3 = T;
    }
    H2[0] = H2[0] + a3 >>> 0;
    H2[1] = H2[1] + b2 >>> 0;
    H2[2] = H2[2] + c2 >>> 0;
    H2[3] = H2[3] + d2 >>> 0;
    H2[4] = H2[4] + e2 >>> 0;
  }
  return Uint8Array.of(H2[0] >> 24, H2[0] >> 16, H2[0] >> 8, H2[0], H2[1] >> 24, H2[1] >> 16, H2[1] >> 8, H2[1], H2[2] >> 24, H2[2] >> 16, H2[2] >> 8, H2[2], H2[3] >> 24, H2[3] >> 16, H2[3] >> 8, H2[3], H2[4] >> 24, H2[4] >> 16, H2[4] >> 8, H2[4]);
}
var sha1_default = sha1;

// node_modules/uuid/dist/v5.js
function v5(value, namespace, buf, offset) {
  return v35(80, sha1_default, value, namespace, buf, offset);
}
v5.DNS = DNS;
v5.URL = URL;

// node_modules/@annotorious/core/dist/annotorious-core.es.js
var W = Object.prototype.hasOwnProperty;
function _(e2, t2) {
  var n2, o2;
  if (e2 === t2) return true;
  if (e2 && t2 && (n2 = e2.constructor) === t2.constructor) {
    if (n2 === Date) return e2.getTime() === t2.getTime();
    if (n2 === RegExp) return e2.toString() === t2.toString();
    if (n2 === Array) {
      if ((o2 = e2.length) === t2.length)
        for (; o2-- && _(e2[o2], t2[o2]); ) ;
      return o2 === -1;
    }
    if (!n2 || typeof e2 == "object") {
      o2 = 0;
      for (n2 in e2)
        if (W.call(e2, n2) && ++o2 && !W.call(t2, n2) || !(n2 in t2) || !_(e2[n2], t2[n2])) return false;
      return Object.keys(t2).length === o2;
    }
  }
  return e2 !== e2 && t2 !== t2;
}
var K = /* @__PURE__ */ Symbol("clean");
var N = [];
var I = 0;
var $ = 4;
var ee = globalThis.nanostoresGlobal || (globalThis.nanostoresGlobal = { epoch: 0 });
var te = () => {
  for (I = 0; I < N.length; I += $)
    N[I](
      N[I + 1].value,
      N[I + 2],
      N[I + 3]
    );
  N.length = 0;
};
var M = (e2) => {
  let t2 = [], n2 = {
    get() {
      return n2.lc || n2.listen(() => {
      })(), n2.value;
    },
    init: e2,
    lc: 0,
    listen(o2) {
      return n2.lc = t2.push(o2), () => {
        for (let f3 = I + $; f3 < N.length; )
          N[f3] === o2 ? N.splice(f3, $) : f3 += $;
        let d2 = t2.indexOf(o2);
        ~d2 && (t2.splice(d2, 1), --n2.lc || n2.off());
      };
    },
    notify(o2, d2) {
      ee.epoch++;
      let f3 = !N.length && true;
      for (let A of t2)
        N.push(
          A,
          n2,
          o2,
          d2
        );
      f3 && te();
    },
    /* It will be called on last listener unsubscribing.
       We will redefine it in onMount and onStop. */
    off() {
    },
    set(o2) {
      let d2 = n2.value;
      d2 !== o2 && (n2.value = o2, n2.notify(d2));
    },
    subscribe(o2) {
      let d2 = n2.listen(o2);
      return o2(n2.value), d2;
    },
    value: e2
  };
  return n2[K] = () => {
    t2 = [], n2.lc = 0, n2.off();
  }, n2;
};
var De = (e2) => {
  const t2 = M(null);
  return e2.observe(({ changes: n2 }) => {
    const o2 = t2.get();
    if (o2) {
      (n2.deleted || []).some((A) => A.id === o2) && t2.set(null);
      const f3 = (n2.updated || []).find(({ oldValue: A }) => A.id === o2);
      f3 && t2.set(f3.newValue.id);
    }
  }), {
    get current() {
      return t2.get();
    },
    subscribe: t2.subscribe.bind(t2),
    set: t2.set.bind(t2)
  };
};
var ne = ((e2) => (e2.EDIT = "EDIT", e2.SELECT = "SELECT", e2.NONE = "NONE", e2))(ne || {});
var z = { selected: [] };
var Ne = (e2, t2, n2) => {
  const o2 = M(z);
  let d2 = t2;
  const f3 = () => {
    _(o2.get(), z) || o2.set(z);
  }, A = () => {
    var g2;
    return ((g2 = o2.get().selected) == null ? void 0 : g2.length) === 0;
  }, w2 = (g2) => {
    if (A())
      return false;
    const E = typeof g2 == "string" ? g2 : g2.id;
    return o2.get().selected.some((L) => L.id === E);
  }, C = (g2, E) => {
    let L;
    if (Array.isArray(g2)) {
      if (L = g2.map((i2) => e2.getAnnotation(i2)).filter(Boolean), L.length < g2.length) {
        console.warn("Invalid selection: " + g2.filter((i2) => !L.some((u2) => u2.id === i2)));
        return;
      }
    } else {
      const i2 = e2.getAnnotation(g2);
      if (!i2) {
        console.warn("Invalid selection: " + g2);
        return;
      }
      L = [i2];
    }
    const O = L.reduce((i2, u2) => {
      const h2 = b2(u2);
      return h2 === "EDIT" ? [...i2, { id: u2.id, editable: true }] : h2 === "SELECT" ? [...i2, { id: u2.id }] : i2;
    }, []);
    o2.set({ selected: O, event: E });
  }, m2 = (g2, E) => {
    const L = Array.isArray(g2) ? g2 : [g2], O = L.map((i2) => e2.getAnnotation(i2)).filter((i2) => !!i2);
    o2.set({
      selected: O.map((i2) => {
        const u2 = E === void 0 ? b2(i2) === "EDIT" : E;
        return { id: i2.id, editable: u2 };
      })
    }), O.length !== L.length && console.warn("Invalid selection", g2);
  }, S = (g2) => {
    if (A())
      return false;
    const { selected: E } = o2.get();
    E.some(({ id: O }) => g2.includes(O)) && o2.set({ selected: E.filter(({ id: O }) => !g2.includes(O)) });
  }, l2 = (g2) => {
    d2 = g2, m2(o2.get().selected.map(({ id: E }) => E));
  }, b2 = (g2) => oe(g2, d2, n2);
  return e2.observe(
    ({ changes: g2 }) => S((g2.deleted || []).map((E) => E.id))
  ), {
    get event() {
      const g2 = o2.get();
      return g2 ? g2.event : null;
    },
    get selected() {
      const g2 = o2.get();
      return g2 ? [...g2.selected] : null;
    },
    get userSelectAction() {
      return d2;
    },
    clear: f3,
    evalSelectAction: b2,
    isEmpty: A,
    isSelected: w2,
    setSelected: m2,
    setUserSelectAction: l2,
    subscribe: o2.subscribe.bind(o2),
    userSelect: C
  };
};
var oe = (e2, t2, n2) => {
  const o2 = n2 ? n2.serialize(e2) : e2;
  return typeof t2 == "function" ? t2(o2) : t2 || "EDIT";
};
var B = [];
for (let e2 = 0; e2 < 256; ++e2)
  B.push((e2 + 256).toString(16).slice(1));
function se(e2, t2 = 0) {
  return (B[e2[t2 + 0]] + B[e2[t2 + 1]] + B[e2[t2 + 2]] + B[e2[t2 + 3]] + "-" + B[e2[t2 + 4]] + B[e2[t2 + 5]] + "-" + B[e2[t2 + 6]] + B[e2[t2 + 7]] + "-" + B[e2[t2 + 8]] + B[e2[t2 + 9]] + "-" + B[e2[t2 + 10]] + B[e2[t2 + 11]] + B[e2[t2 + 12]] + B[e2[t2 + 13]] + B[e2[t2 + 14]] + B[e2[t2 + 15]]).toLowerCase();
}
var ie = new Uint8Array(16);
function de() {
  return crypto.getRandomValues(ie);
}
function Q(e2, t2, n2) {
  return crypto.randomUUID ? crypto.randomUUID() : re(e2);
}
function re(e2, t2, n2) {
  var d2;
  e2 = e2 || {};
  const o2 = e2.random ?? ((d2 = e2.rng) == null ? void 0 : d2.call(e2)) ?? de();
  if (o2.length < 16)
    throw new Error("Random bytes length must be >= 16");
  return o2[6] = o2[6] & 15 | 64, o2[8] = o2[8] & 63 | 128, se(o2);
}
var V = (e2) => {
  const t2 = (n2) => {
    const o2 = { ...n2 };
    return n2.created && typeof n2.created == "string" && (o2.created = new Date(n2.created)), n2.updated && typeof n2.updated == "string" && (o2.updated = new Date(n2.updated)), o2;
  };
  return {
    ...e2,
    bodies: (e2.bodies || []).map(t2),
    target: t2(e2.target)
  };
};
var Ie = (e2, t2, n2, o2) => ({
  id: Q(),
  annotation: typeof e2 == "string" ? e2 : e2.id,
  created: n2 || /* @__PURE__ */ new Date(),
  creator: o2,
  ...t2
});
var ae = (e2, t2) => {
  const n2 = new Set(e2.bodies.map((o2) => o2.id));
  return t2.bodies.filter((o2) => !n2.has(o2.id));
};
var ce = (e2, t2) => {
  const n2 = new Set(t2.bodies.map((o2) => o2.id));
  return e2.bodies.filter((o2) => !n2.has(o2.id));
};
var le = (e2, t2) => t2.bodies.map((n2) => {
  const o2 = e2.bodies.find((d2) => d2.id === n2.id);
  return { newBody: n2, oldBody: o2 && !_(o2, n2) ? o2 : void 0 };
}).filter(({ oldBody: n2 }) => n2).map(({ oldBody: n2, newBody: o2 }) => ({ oldBody: n2, newBody: o2 }));
var ue = (e2, t2) => !_(e2.target, t2.target);
var q = (e2, t2) => {
  const n2 = ae(e2, t2), o2 = ce(e2, t2), d2 = le(e2, t2);
  return {
    oldValue: e2,
    newValue: t2,
    bodiesCreated: n2.length > 0 ? n2 : void 0,
    bodiesDeleted: o2.length > 0 ? o2 : void 0,
    bodiesUpdated: d2.length > 0 ? d2 : void 0,
    targetUpdated: ue(e2, t2) ? { oldTarget: e2.target, newTarget: t2.target } : void 0
  };
};
var fe = ((e2) => (e2.BODY_ONLY = "BODY_ONLY", e2.TARGET_ONLY = "TARGET_ONLY", e2))(fe || {});
var y = ((e2) => (e2.LOCAL = "LOCAL", e2.REMOTE = "REMOTE", e2.SILENT = "SILENT", e2))(y || {});
var pe = (e2, t2) => {
  var f3, A;
  const { changes: n2, origin: o2 } = t2;
  if (!(e2.options.origin ? e2.options.origin === o2 : o2 !== "SILENT"))
    return false;
  if (e2.options.ignore) {
    const { ignore: w2 } = e2.options, C = (S) => S && S.length > 0;
    if (!(C(n2.created) || C(n2.deleted))) {
      const S = (f3 = n2.updated) == null ? void 0 : f3.some((b2) => C(b2.bodiesCreated) || C(b2.bodiesDeleted) || C(b2.bodiesUpdated)), l2 = (A = n2.updated) == null ? void 0 : A.some((b2) => b2.targetUpdated);
      if (w2 === "BODY_ONLY" && S && !l2 || w2 === "TARGET_ONLY" && l2 && !S)
        return false;
    }
  }
  if (e2.options.annotations) {
    const w2 = /* @__PURE__ */ new Set([
      ...(n2.created || []).map((m2) => m2.id),
      ...(n2.deleted || []).map((m2) => m2.id),
      ...(n2.updated || []).map(({ oldValue: m2 }) => m2.id)
    ]);
    return !!(Array.isArray(e2.options.annotations) ? e2.options.annotations : [e2.options.annotations]).find((m2) => w2.has(m2));
  } else
    return true;
};
var ge = (e2, t2) => {
  const n2 = new Set((e2.created || []).map((l2) => l2.id)), o2 = new Set((e2.updated || []).map(({ newValue: l2 }) => l2.id)), d2 = new Set((t2.created || []).map((l2) => l2.id)), f3 = new Set((t2.deleted || []).map((l2) => l2.id)), A = new Set((t2.updated || []).map(({ oldValue: l2 }) => l2.id)), w2 = new Set((t2.updated || []).filter(({ oldValue: l2 }) => n2.has(l2.id) || o2.has(l2.id)).map(({ oldValue: l2 }) => l2.id)), C = [
    ...(e2.created || []).filter((l2) => !f3.has(l2.id)).map((l2) => A.has(l2.id) ? t2.updated.find(({ oldValue: b2 }) => b2.id === l2.id).newValue : l2),
    ...t2.created || []
  ], m2 = [
    ...(e2.deleted || []).filter((l2) => !d2.has(l2.id)),
    ...(t2.deleted || []).filter((l2) => !n2.has(l2.id))
  ], S = [
    ...(e2.updated || []).filter(({ newValue: l2 }) => !f3.has(l2.id)).map((l2) => {
      const { oldValue: b2, newValue: g2 } = l2;
      if (A.has(g2.id)) {
        const E = t2.updated.find((L) => L.oldValue.id === g2.id).newValue;
        return q(b2, E);
      } else
        return l2;
    }),
    ...(t2.updated || []).filter(({ oldValue: l2 }) => !w2.has(l2.id))
  ];
  return { created: C, deleted: m2, updated: S };
};
var P = (e2) => {
  const t2 = e2.id === void 0 ? Q() : e2.id;
  return {
    ...e2,
    id: t2,
    bodies: e2.bodies === void 0 ? [] : e2.bodies.map((n2) => ({
      ...n2,
      annotation: t2
    })),
    target: {
      ...e2.target,
      annotation: t2
    }
  };
};
var he = (e2) => e2.id !== void 0;
var _e = () => {
  const e2 = /* @__PURE__ */ new Map(), t2 = /* @__PURE__ */ new Map(), n2 = [], o2 = (s2, r2 = {}) => {
    n2.push({ onChange: s2, options: r2 });
  }, d2 = (s2) => {
    const r2 = n2.findIndex((a3) => a3.onChange == s2);
    r2 > -1 && n2.splice(r2, 1);
  }, f3 = (s2, r2) => {
    const a3 = {
      origin: s2,
      changes: {
        created: r2.created || [],
        updated: r2.updated || [],
        deleted: r2.deleted || []
      },
      state: [...e2.values()]
    };
    n2.forEach((p2) => {
      pe(p2, a3) && p2.onChange(a3);
    });
  }, A = (s2) => {
    e2.set(s2.id, s2), s2.bodies.forEach((r2) => t2.set(r2.id, s2.id));
  }, w2 = (s2, r2 = y.LOCAL) => {
    if (s2.id && e2.get(s2.id))
      throw Error(`Cannot add annotation ${s2.id} - exists already`);
    {
      const p2 = P(s2);
      A(p2), f3(r2, { created: [p2] });
    }
  }, C = (s2, r2) => {
    const a3 = P(typeof s2 == "string" ? r2 : s2), p2 = typeof s2 == "string" ? s2 : s2.id, T = p2 && e2.get(p2);
    if (T) {
      const R = q(T, a3);
      return p2 === a3.id ? e2.set(p2, a3) : (e2.delete(p2), e2.set(a3.id, a3)), T.bodies.forEach((Y) => t2.delete(Y.id)), a3.bodies.forEach((Y) => t2.set(Y.id, a3.id)), R;
    } else
      console.warn(`Cannot update annotation ${p2} - does not exist`);
  }, m2 = (s2, r2 = y.LOCAL, a3 = y.LOCAL) => {
    const p2 = he(r2) ? a3 : r2, T = C(s2, r2);
    T && f3(p2, { updated: [T] });
  }, S = (s2, r2 = y.LOCAL) => {
    !!e2.get(s2.id) ? m2(s2, r2) : w2(s2, r2);
  }, l2 = (s2, r2 = y.LOCAL) => {
    const a3 = s2.reduce((p2, T) => {
      const R = C(T);
      return R ? [...p2, R] : p2;
    }, []);
    a3.length > 0 && f3(r2, { updated: a3 });
  }, b2 = (s2) => s2.map(P).reduce((a3, p2) => e2.get(p2.id) ? { ...a3, toUpdate: [...a3.toUpdate, p2] } : { ...a3, toAdd: [...a3.toAdd, p2] }, { toAdd: [], toUpdate: [] }), g2 = (s2, r2 = y.LOCAL) => {
    const { toAdd: a3, toUpdate: p2 } = b2(s2), T = p2.map((R) => C(R, r2)).filter(Boolean);
    a3.forEach(A), f3(r2, { created: a3, updated: T });
  }, E = (s2, r2 = y.LOCAL) => {
    const a3 = e2.get(s2.annotation);
    if (a3) {
      const p2 = {
        ...a3,
        bodies: [...a3.bodies, s2]
      };
      e2.set(a3.id, p2), t2.set(s2.id, p2.id), f3(r2, { updated: [{
        oldValue: a3,
        newValue: p2,
        bodiesCreated: [s2]
      }] });
    } else
      console.warn(`Attempt to add body to missing annotation: ${s2.annotation}`);
  }, L = () => [...e2.values()], O = (s2 = y.LOCAL) => {
    const r2 = [...e2.values()];
    e2.clear(), t2.clear(), f3(s2, { deleted: r2 });
  }, i2 = (s2, r2 = y.LOCAL) => {
    const { toAdd: a3, toUpdate: p2 } = b2(s2), T = new Set([...a3, ...p2].map((k) => k.id)), R = [...e2.keys()].filter((k) => !T.has(k)).map((k) => h2(k)).filter(Boolean), Y = p2.map((k) => C(k)).filter(Boolean);
    a3.forEach(A), f3(r2, { created: a3, updated: Y, deleted: R });
  }, u2 = (s2, r2 = true, a3 = y.LOCAL) => {
    r2 ? i2(s2, a3) : g2(s2, a3);
  }, h2 = (s2) => {
    const r2 = typeof s2 == "string" ? s2 : s2.id, a3 = e2.get(r2);
    if (a3)
      return e2.delete(r2), a3.bodies.forEach((p2) => t2.delete(p2.id)), a3;
    console.warn(`Attempt to delete missing annotation: ${r2}`);
  }, v2 = (s2, r2 = y.LOCAL) => {
    const a3 = h2(s2);
    a3 && f3(r2, { deleted: [a3] });
  }, c2 = (s2, r2 = y.LOCAL) => {
    const a3 = s2.reduce((p2, T) => {
      const R = h2(T);
      return R ? [...p2, R] : p2;
    }, []);
    a3.length > 0 && f3(r2, { deleted: a3 });
  }, U2 = (s2) => {
    const r2 = e2.get(s2.annotation);
    if (r2) {
      const a3 = r2.bodies.find((p2) => p2.id === s2.id);
      if (a3) {
        t2.delete(a3.id);
        const p2 = {
          ...r2,
          bodies: r2.bodies.filter((R) => R.id !== s2.id)
        };
        return e2.set(r2.id, p2), {
          oldValue: r2,
          newValue: p2,
          bodiesDeleted: [a3]
        };
      } else
        console.warn(`Attempt to delete missing body ${s2.id} from annotation ${s2.annotation}`);
    } else
      console.warn(`Attempt to delete body from missing annotation ${s2.annotation}`);
  }, D = (s2, r2 = y.LOCAL) => {
    const a3 = U2(s2);
    a3 && f3(r2, { updated: [a3] });
  }, x2 = (s2, r2 = y.LOCAL) => {
    const a3 = s2.map((p2) => U2(p2)).filter(Boolean);
    a3.length > 0 && f3(r2, { updated: a3 });
  }, j2 = (s2) => {
    const r2 = e2.get(s2);
    return r2 ? { ...r2 } : void 0;
  }, X = (s2) => {
    const r2 = t2.get(s2);
    if (r2) {
      const p2 = j2(r2).bodies.find((T) => T.id === s2);
      if (p2)
        return p2;
      console.error(`Store integrity error: body ${s2} in index, but not in annotation`);
    } else
      console.warn(`Attempt to retrieve missing body: ${s2}`);
  }, G = (s2, r2) => {
    if (s2.annotation !== r2.annotation)
      throw "Annotation integrity violation: annotation ID must be the same when updating bodies";
    const a3 = e2.get(s2.annotation);
    if (a3) {
      const p2 = a3.bodies.find((R) => R.id === s2.id), T = {
        ...a3,
        bodies: a3.bodies.map((R) => R.id === p2.id ? r2 : R)
      };
      return e2.set(a3.id, T), p2.id !== r2.id && (t2.delete(p2.id), t2.set(r2.id, T.id)), {
        oldValue: a3,
        newValue: T,
        bodiesUpdated: [{ oldBody: p2, newBody: r2 }]
      };
    } else
      console.warn(`Attempt to add body to missing annotation ${s2.annotation}`);
  }, Z2 = (s2, r2, a3 = y.LOCAL) => {
    const p2 = G(s2, r2);
    p2 && f3(a3, { updated: [p2] });
  }, F = (s2, r2 = y.LOCAL) => {
    const a3 = s2.map((p2) => G({ id: p2.id, annotation: p2.annotation }, p2)).filter(Boolean);
    f3(r2, { updated: a3 });
  }, H2 = (s2) => {
    const r2 = e2.get(s2.annotation);
    if (r2) {
      const a3 = {
        ...r2,
        target: {
          ...r2.target,
          ...s2
        }
      };
      return e2.set(r2.id, a3), {
        oldValue: r2,
        newValue: a3,
        targetUpdated: {
          oldTarget: r2.target,
          newTarget: s2
        }
      };
    } else
      console.warn(`Attempt to update target on missing annotation: ${s2.annotation}`);
  };
  return {
    addAnnotation: w2,
    addBody: E,
    all: L,
    bulkAddAnnotations: u2,
    bulkDeleteAnnotations: c2,
    bulkDeleteBodies: x2,
    bulkUpdateAnnotations: l2,
    bulkUpdateBodies: F,
    bulkUpdateTargets: (s2, r2 = y.LOCAL) => {
      const a3 = s2.map((p2) => H2(p2)).filter(Boolean);
      a3.length > 0 && f3(r2, { updated: a3 });
    },
    bulkUpsertAnnotations: g2,
    clear: O,
    deleteAnnotation: v2,
    deleteBody: D,
    getAnnotation: j2,
    getBody: X,
    observe: o2,
    syncAnnotations: i2,
    unobserve: d2,
    updateAnnotation: m2,
    updateBody: Z2,
    updateTarget: (s2, r2 = y.LOCAL) => {
      const a3 = H2(s2);
      a3 && f3(r2, { updated: [a3] });
    },
    upsertAnnotation: S
  };
};
var J = () => ({
  emit(e2, ...t2) {
    for (let n2 = this.events[e2] || [], o2 = 0, d2 = n2.length; o2 < d2; o2++)
      n2[o2](...t2);
  },
  events: {},
  on(e2, t2) {
    var n2;
    return ((n2 = this.events)[e2] || (n2[e2] = [])).push(t2), () => {
      var o2;
      this.events[e2] = (o2 = this.events[e2]) == null ? void 0 : o2.filter((d2) => t2 !== d2);
    };
  }
});
var Ae = 250;
var ke = (e2, t2) => {
  const n2 = J(), o2 = (t2 == null ? void 0 : t2.changes) || [];
  let d2 = t2 ? t2.pointer : -1, f3 = false, A = 0;
  const w2 = (c2) => {
    if (!f3) {
      const { changes: U2 } = c2, D = performance.now();
      if (D - A > Ae)
        o2.splice(d2 + 1), o2.push(U2), d2 = o2.length - 1;
      else {
        const x2 = o2.length - 1;
        o2[x2] = ge(o2[x2], U2);
      }
      A = D;
    }
    f3 = false;
  };
  e2.observe(w2, { origin: y.LOCAL });
  const C = (c2) => c2 && c2.length > 0 && e2.bulkDeleteAnnotations(c2), m2 = (c2) => c2 && c2.length > 0 && e2.bulkUpsertAnnotations(c2), S = (c2) => c2 && c2.length > 0 && e2.bulkUpdateAnnotations(c2.map(({ oldValue: U2 }) => U2)), l2 = (c2) => c2 && c2.length > 0 && e2.bulkUpdateAnnotations(c2.map(({ newValue: U2 }) => U2)), b2 = (c2) => c2 && c2.length > 0 && e2.bulkUpsertAnnotations(c2), g2 = (c2) => c2 && c2.length > 0 && e2.bulkDeleteAnnotations(c2);
  return {
    canRedo: () => o2.length - 1 > d2,
    canUndo: () => d2 > -1,
    destroy: () => e2.unobserve(w2),
    getHistory: () => ({ changes: [...o2], pointer: d2 }),
    on: (c2, U2) => n2.on(c2, U2),
    redo: () => {
      if (o2.length - 1 > d2) {
        f3 = true;
        const { created: c2, updated: U2, deleted: D } = o2[d2 + 1];
        m2(c2), l2(U2), g2(D), n2.emit("redo", o2[d2 + 1]), d2 += 1;
      }
    },
    undo: () => {
      if (d2 > -1) {
        f3 = true;
        const { created: c2, updated: U2, deleted: D } = o2[d2];
        C(c2), S(U2), b2(D), n2.emit("undo", o2[d2]), d2 -= 1;
      }
    }
  };
};
var Ye = () => {
  const e2 = M([]);
  return {
    subscribe: e2.subscribe.bind(e2),
    set: e2.set.bind(e2)
  };
};
var Pe = (e2, t2, n2, o2) => {
  const { hover: d2, selection: f3, store: A, viewport: w2 } = e2, C = /* @__PURE__ */ new Map();
  let m2 = [], S, l2;
  const b2 = (i2, u2) => {
    C.has(i2) ? C.get(i2).push(u2) : C.set(i2, [u2]);
  }, g2 = (i2, u2) => {
    const h2 = C.get(i2);
    if (h2) {
      const v2 = h2.indexOf(u2);
      v2 !== -1 && h2.splice(v2, 1);
    }
  }, E = (i2, u2, h2) => {
    C.has(i2) && setTimeout(() => {
      C.get(i2).forEach((v2) => {
        if (n2) {
          const c2 = Array.isArray(u2) ? u2.map((D) => n2.serialize(D)) : n2.serialize(u2), U2 = h2 ? h2 instanceof PointerEvent ? h2 : n2.serialize(h2) : void 0;
          v2(c2, U2);
        } else
          v2(u2, h2);
      });
    }, 1);
  }, L = () => {
    const { selected: i2 } = f3, u2 = (i2 || []).map(({ id: h2 }) => A.getAnnotation(h2));
    u2.forEach((h2) => {
      const v2 = m2.find((c2) => c2.id === h2.id);
      (!v2 || !_(v2, h2)) && E("updateAnnotation", h2, v2);
    }), m2 = m2.map((h2) => {
      const v2 = u2.find(({ id: c2 }) => c2 === h2.id);
      return v2 || h2;
    });
  };
  f3.subscribe(({ selected: i2 }) => {
    if (!(m2.length === 0 && i2.length === 0)) {
      if (m2.length === 0 && i2.length > 0)
        m2 = i2.map(({ id: u2 }) => A.getAnnotation(u2));
      else if (m2.length > 0 && i2.length === 0)
        m2.forEach((u2) => {
          const h2 = A.getAnnotation(u2.id);
          h2 && !_(h2, u2) && E("updateAnnotation", h2, u2);
        }), m2 = [];
      else {
        const u2 = new Set(m2.map((c2) => c2.id)), h2 = new Set(i2.map(({ id: c2 }) => c2));
        m2.filter((c2) => !h2.has(c2.id)).forEach((c2) => {
          const U2 = A.getAnnotation(c2.id);
          U2 && !_(U2, c2) && E("updateAnnotation", U2, c2);
        }), m2 = [
          // Remove annotations that were deselected
          ...m2.filter((c2) => h2.has(c2.id)),
          // Add editable annotations that were selected
          ...i2.filter(({ id: c2 }) => !u2.has(c2)).map(({ id: c2 }) => A.getAnnotation(c2))
        ];
      }
      E("selectionChanged", m2);
    }
  }), d2.subscribe((i2) => {
    !S && i2 ? E("mouseEnterAnnotation", A.getAnnotation(i2)) : S && !i2 ? E("mouseLeaveAnnotation", A.getAnnotation(S)) : S && i2 && (E("mouseLeaveAnnotation", A.getAnnotation(S)), E("mouseEnterAnnotation", A.getAnnotation(i2))), S = i2;
  }), w2 == null || w2.subscribe((i2) => E("viewportIntersect", i2.map((u2) => A.getAnnotation(u2)))), A.observe((i2) => {
    o2 && (l2 && clearTimeout(l2), l2 = setTimeout(L, 1e3));
    const { created: u2, deleted: h2 } = i2.changes;
    (u2 || []).forEach((c2) => E("createAnnotation", c2)), (h2 || []).forEach((c2) => E("deleteAnnotation", c2)), (i2.changes.updated || []).filter((c2) => [
      ...c2.bodiesCreated || [],
      ...c2.bodiesDeleted || [],
      ...c2.bodiesUpdated || []
    ].length > 0).forEach(({ oldValue: c2, newValue: U2 }) => {
      const D = m2.find((x2) => x2.id === c2.id) || c2;
      m2 = m2.map((x2) => x2.id === c2.id ? U2 : x2), E("updateAnnotation", U2, D);
    });
  }, { origin: y.LOCAL }), A.observe((i2) => {
    if (m2) {
      const u2 = new Set(m2.map((v2) => v2.id)), h2 = (i2.changes.updated || []).filter(({ newValue: v2 }) => u2.has(v2.id)).map(({ newValue: v2 }) => v2);
      h2.length > 0 && (m2 = m2.map((v2) => {
        const c2 = h2.find((U2) => U2.id === v2.id);
        return c2 || v2;
      }));
    }
  }, { origin: y.REMOTE });
  const O = (i2) => (u2) => {
    const { updated: h2 } = u2;
    i2 ? (h2 || []).forEach((v2) => E("updateAnnotation", v2.oldValue, v2.newValue)) : (h2 || []).forEach((v2) => E("updateAnnotation", v2.newValue, v2.oldValue));
  };
  return t2.on("undo", O(true)), t2.on("redo", O(false)), { on: b2, off: g2, emit: E };
};
var me = (e2) => (t2) => t2.reduce((n2, o2) => {
  const { parsed: d2, error: f3 } = e2.parse(o2);
  return f3 ? {
    parsed: n2.parsed,
    failed: [...n2.failed, o2]
  } : d2 ? {
    parsed: [...n2.parsed, d2],
    failed: n2.failed
  } : {
    ...n2
  };
}, { parsed: [], failed: [] });
var ze = (e2, t2, n2) => {
  const { store: o2, selection: d2 } = e2, f3 = (i2) => {
    if (n2) {
      const { parsed: u2, error: h2 } = n2.parse(i2);
      u2 ? o2.addAnnotation(u2, y.REMOTE) : console.error(h2);
    } else
      o2.addAnnotation(V(i2), y.REMOTE);
  }, A = () => d2.clear(), w2 = () => o2.clear(), C = (i2) => {
    const u2 = o2.getAnnotation(i2);
    return n2 && u2 ? n2.serialize(u2) : u2;
  }, m2 = () => n2 ? o2.all().map(n2.serialize) : o2.all(), S = () => {
    var h2;
    const u2 = (((h2 = d2.selected) == null ? void 0 : h2.map((v2) => v2.id)) || []).map((v2) => o2.getAnnotation(v2)).filter(Boolean);
    return n2 ? u2.map(n2.serialize) : u2;
  }, l2 = (i2, u2 = true) => fetch(i2).then((h2) => h2.json()).then((h2) => (g2(h2, u2), h2)), b2 = (i2) => {
    if (typeof i2 == "string") {
      const u2 = o2.getAnnotation(i2);
      if (o2.deleteAnnotation(i2), u2)
        return n2 ? n2.serialize(u2) : u2;
    } else {
      const u2 = n2 ? n2.parse(i2).parsed : i2;
      if (u2)
        return o2.deleteAnnotation(u2), i2;
    }
  }, g2 = (i2, u2 = true) => {
    const h2 = u2 ? o2.syncAnnotations : o2.bulkUpsertAnnotations;
    if (n2) {
      const v2 = n2.parseAll || me(n2), { parsed: c2, failed: U2 } = v2(i2);
      U2.length > 0 && console.warn(`Discarded ${U2.length} invalid annotations`, U2), h2(c2, y.REMOTE);
    } else
      h2(i2.map(V), y.REMOTE);
  }, E = (i2, u2) => {
    i2 ? d2.setSelected(i2, u2) : d2.clear();
  }, L = (i2) => {
    d2.setUserSelectAction(i2);
  }, O = (i2) => {
    if (n2) {
      const u2 = n2.parse(i2).parsed, h2 = n2.serialize(o2.getAnnotation(u2.id));
      return o2.updateAnnotation(u2), h2;
    } else {
      const u2 = o2.getAnnotation(i2.id);
      return o2.updateAnnotation(V(i2)), u2;
    }
  };
  return {
    addAnnotation: f3,
    cancelSelected: A,
    canRedo: t2.canRedo,
    canUndo: t2.canUndo,
    clearAnnotations: w2,
    getAnnotationById: C,
    getAnnotations: m2,
    getHistory: t2.getHistory,
    getSelected: S,
    loadAnnotations: l2,
    redo: t2.redo,
    removeAnnotation: b2,
    setAnnotations: g2,
    setSelected: E,
    setUserSelectAction: L,
    undo: t2.undo,
    updateAnnotation: O
  };
};
var Ee = "useandom-26T198340PX75pxJACKVERYMINDBUSHWOLF_GQZbfghjklqvwyzrict";
var be = (e2) => crypto.getRandomValues(new Uint8Array(e2));
var ve = (e2, t2, n2) => {
  let o2 = 256 - 256 % e2.length;
  if (o2 === 256) {
    let f3 = e2.length - 1;
    return (A = t2) => {
      if (!A) return "";
      let w2 = "";
      for (; ; ) {
        let C = n2(A), m2 = A;
        for (; m2--; )
          if (w2 += e2[C[m2] & f3], w2.length >= A) return w2;
      }
    };
  }
  let d2 = Math.ceil(1.6 * 256 * t2 / o2);
  return (f3 = t2) => {
    if (!f3) return "";
    let A = "";
    for (; ; ) {
      let w2 = n2(d2), C = d2;
      for (; C--; )
        if (w2[C] < o2 && (A += e2[w2[C] % e2.length], A.length >= f3))
          return A;
    }
  };
};
var Ce = (e2, t2 = 21) => ve(e2, t2 | 0, be);
var we = (e2 = 21) => {
  let t2 = "", n2 = crypto.getRandomValues(new Uint8Array(e2 |= 0));
  for (; e2--; )
    t2 += Ee[n2[e2] & 63];
  return t2;
};
var je = () => ({ isGuest: true, id: Ce("1234567890abcdefghijklmnopqrstuvwxyzABCDEFGHIJKLMNOPQRSTUVWXYZ_", 20)() });
var Le = (e2) => {
  const t2 = JSON.stringify(e2);
  let n2 = 0;
  for (let o2 = 0, d2 = t2.length; o2 < d2; o2++) {
    let f3 = t2.charCodeAt(o2);
    n2 = (n2 << 5) - n2 + f3, n2 |= 0;
  }
  return `${n2}`;
};
var Ue = (e2) => e2 ? typeof e2 == "object" ? { ...e2 } : e2 : void 0;
var Ge = (e2, t2) => (Array.isArray(e2) ? e2 : [e2]).map((n2) => {
  const { id: o2, type: d2, purpose: f3, value: A, created: w2, modified: C, creator: m2, ...S } = n2;
  return {
    id: o2 || `temp-${Le(n2)}`,
    annotation: t2,
    type: d2,
    purpose: f3,
    value: A,
    creator: Ue(m2),
    created: w2 ? new Date(w2) : void 0,
    updated: C ? new Date(C) : void 0,
    ...S
  };
});
var He = (e2) => e2.map((t2) => {
  var w2;
  const { annotation: n2, created: o2, updated: d2, ...f3 } = t2, A = {
    ...f3,
    created: o2 == null ? void 0 : o2.toISOString(),
    modified: d2 == null ? void 0 : d2.toISOString()
  };
  return (w2 = A.id) != null && w2.startsWith("temp-") && delete A.id, A;
});
globalThis.ANNOTORIOUS_PRESENCE_KEY || (globalThis.ANNOTORIOUS_PRESENCE_KEY = we());
var Qe = globalThis.ANNOTORIOUS_PRESENCE_KEY;

// node_modules/nanoevents/index.js
var createNanoEvents = () => ({
  emit(event, ...args) {
    for (let callbacks = this.events[event] || [], i2 = 0, length = callbacks.length; i2 < length; i2++) {
      callbacks[i2](...args);
    }
  },
  events: {},
  on(event, cb) {
    ;
    (this.events[event] ||= []).push(cb);
    return () => {
      this.events[event] = this.events[event]?.filter((i2) => cb !== i2);
    };
  }
});

// node_modules/colord/index.mjs
var r = { grad: 0.9, turn: 360, rad: 360 / (2 * Math.PI) };
var t = function(r2) {
  return "string" == typeof r2 ? r2.length > 0 : "number" == typeof r2;
};
var n = function(r2, t2, n2) {
  return void 0 === t2 && (t2 = 0), void 0 === n2 && (n2 = Math.pow(10, t2)), Math.round(n2 * r2) / n2 + 0;
};
var e = function(r2, t2, n2) {
  return void 0 === t2 && (t2 = 0), void 0 === n2 && (n2 = 1), r2 > n2 ? n2 : r2 > t2 ? r2 : t2;
};
var u = function(r2) {
  return (r2 = isFinite(r2) ? r2 % 360 : 0) > 0 ? r2 : r2 + 360;
};
var a = function(r2) {
  return { r: e(r2.r, 0, 255), g: e(r2.g, 0, 255), b: e(r2.b, 0, 255), a: e(r2.a) };
};
var o = function(r2) {
  return { r: n(r2.r), g: n(r2.g), b: n(r2.b), a: n(r2.a, 3) };
};
var i = /^#([0-9a-f]{3,8})$/i;
var s = function(r2) {
  var t2 = r2.toString(16);
  return t2.length < 2 ? "0" + t2 : t2;
};
var h = function(r2) {
  var t2 = r2.r, n2 = r2.g, e2 = r2.b, u2 = r2.a, a3 = Math.max(t2, n2, e2), o2 = a3 - Math.min(t2, n2, e2), i2 = o2 ? a3 === t2 ? (n2 - e2) / o2 : a3 === n2 ? 2 + (e2 - t2) / o2 : 4 + (t2 - n2) / o2 : 0;
  return { h: 60 * (i2 < 0 ? i2 + 6 : i2), s: a3 ? o2 / a3 * 100 : 0, v: a3 / 255 * 100, a: u2 };
};
var b = function(r2) {
  var t2 = r2.h, n2 = r2.s, e2 = r2.v, u2 = r2.a;
  t2 = t2 / 360 * 6, n2 /= 100, e2 /= 100;
  var a3 = Math.floor(t2), o2 = e2 * (1 - n2), i2 = e2 * (1 - (t2 - a3) * n2), s2 = e2 * (1 - (1 - t2 + a3) * n2), h2 = a3 % 6;
  return { r: 255 * [e2, i2, o2, o2, s2, e2][h2], g: 255 * [s2, e2, e2, i2, o2, o2][h2], b: 255 * [o2, o2, s2, e2, e2, i2][h2], a: u2 };
};
var g = function(r2) {
  return { h: u(r2.h), s: e(r2.s, 0, 100), l: e(r2.l, 0, 100), a: e(r2.a) };
};
var d = function(r2) {
  return { h: n(r2.h), s: n(r2.s), l: n(r2.l), a: n(r2.a, 3) };
};
var f2 = function(r2) {
  return b((n2 = (t2 = r2).s, { h: t2.h, s: (n2 *= ((e2 = t2.l) < 50 ? e2 : 100 - e2) / 100) > 0 ? 2 * n2 / (e2 + n2) * 100 : 0, v: e2 + n2, a: t2.a }));
  var t2, n2, e2;
};
var c = function(r2) {
  return { h: (t2 = h(r2)).h, s: (u2 = (200 - (n2 = t2.s)) * (e2 = t2.v) / 100) > 0 && u2 < 200 ? n2 * e2 / 100 / (u2 <= 100 ? u2 : 200 - u2) * 100 : 0, l: u2 / 2, a: t2.a };
  var t2, n2, e2, u2;
};
var l = /^hsla?\(\s*([+-]?\d*\.?\d+)(deg|rad|grad|turn)?\s*,\s*([+-]?\d*\.?\d+)%\s*,\s*([+-]?\d*\.?\d+)%\s*(?:,\s*([+-]?\d*\.?\d+)(%)?\s*)?\)$/i;
var p = /^hsla?\(\s*([+-]?\d*\.?\d+)(deg|rad|grad|turn)?\s+([+-]?\d*\.?\d+)%\s+([+-]?\d*\.?\d+)%\s*(?:\/\s*([+-]?\d*\.?\d+)(%)?\s*)?\)$/i;
var v = /^rgba?\(\s*([+-]?\d*\.?\d+)(%)?\s*,\s*([+-]?\d*\.?\d+)(%)?\s*,\s*([+-]?\d*\.?\d+)(%)?\s*(?:,\s*([+-]?\d*\.?\d+)(%)?\s*)?\)$/i;
var m = /^rgba?\(\s*([+-]?\d*\.?\d+)(%)?\s+([+-]?\d*\.?\d+)(%)?\s+([+-]?\d*\.?\d+)(%)?\s*(?:\/\s*([+-]?\d*\.?\d+)(%)?\s*)?\)$/i;
var y2 = { string: [[function(r2) {
  var t2 = i.exec(r2);
  return t2 ? (r2 = t2[1]).length <= 4 ? { r: parseInt(r2[0] + r2[0], 16), g: parseInt(r2[1] + r2[1], 16), b: parseInt(r2[2] + r2[2], 16), a: 4 === r2.length ? n(parseInt(r2[3] + r2[3], 16) / 255, 2) : 1 } : 6 === r2.length || 8 === r2.length ? { r: parseInt(r2.substr(0, 2), 16), g: parseInt(r2.substr(2, 2), 16), b: parseInt(r2.substr(4, 2), 16), a: 8 === r2.length ? n(parseInt(r2.substr(6, 2), 16) / 255, 2) : 1 } : null : null;
}, "hex"], [function(r2) {
  var t2 = v.exec(r2) || m.exec(r2);
  return t2 ? t2[2] !== t2[4] || t2[4] !== t2[6] ? null : a({ r: Number(t2[1]) / (t2[2] ? 100 / 255 : 1), g: Number(t2[3]) / (t2[4] ? 100 / 255 : 1), b: Number(t2[5]) / (t2[6] ? 100 / 255 : 1), a: void 0 === t2[7] ? 1 : Number(t2[7]) / (t2[8] ? 100 : 1) }) : null;
}, "rgb"], [function(t2) {
  var n2 = l.exec(t2) || p.exec(t2);
  if (!n2) return null;
  var e2, u2, a3 = g({ h: (e2 = n2[1], u2 = n2[2], void 0 === u2 && (u2 = "deg"), Number(e2) * (r[u2] || 1)), s: Number(n2[3]), l: Number(n2[4]), a: void 0 === n2[5] ? 1 : Number(n2[5]) / (n2[6] ? 100 : 1) });
  return f2(a3);
}, "hsl"]], object: [[function(r2) {
  var n2 = r2.r, e2 = r2.g, u2 = r2.b, o2 = r2.a, i2 = void 0 === o2 ? 1 : o2;
  return t(n2) && t(e2) && t(u2) ? a({ r: Number(n2), g: Number(e2), b: Number(u2), a: Number(i2) }) : null;
}, "rgb"], [function(r2) {
  var n2 = r2.h, e2 = r2.s, u2 = r2.l, a3 = r2.a, o2 = void 0 === a3 ? 1 : a3;
  if (!t(n2) || !t(e2) || !t(u2)) return null;
  var i2 = g({ h: Number(n2), s: Number(e2), l: Number(u2), a: Number(o2) });
  return f2(i2);
}, "hsl"], [function(r2) {
  var n2 = r2.h, a3 = r2.s, o2 = r2.v, i2 = r2.a, s2 = void 0 === i2 ? 1 : i2;
  if (!t(n2) || !t(a3) || !t(o2)) return null;
  var h2 = (function(r3) {
    return { h: u(r3.h), s: e(r3.s, 0, 100), v: e(r3.v, 0, 100), a: e(r3.a) };
  })({ h: Number(n2), s: Number(a3), v: Number(o2), a: Number(s2) });
  return b(h2);
}, "hsv"]] };
var N2 = function(r2, t2) {
  for (var n2 = 0; n2 < t2.length; n2++) {
    var e2 = t2[n2][0](r2);
    if (e2) return [e2, t2[n2][1]];
  }
  return [null, void 0];
};
var x = function(r2) {
  return "string" == typeof r2 ? N2(r2.trim(), y2.string) : "object" == typeof r2 && null !== r2 ? N2(r2, y2.object) : [null, void 0];
};
var M2 = function(r2, t2) {
  var n2 = c(r2);
  return { h: n2.h, s: e(n2.s + 100 * t2, 0, 100), l: n2.l, a: n2.a };
};
var H = function(r2) {
  return (299 * r2.r + 587 * r2.g + 114 * r2.b) / 1e3 / 255;
};
var $2 = function(r2, t2) {
  var n2 = c(r2);
  return { h: n2.h, s: n2.s, l: e(n2.l + 100 * t2, 0, 100), a: n2.a };
};
var j = (function() {
  function r2(r3) {
    this.parsed = x(r3)[0], this.rgba = this.parsed || { r: 0, g: 0, b: 0, a: 1 };
  }
  return r2.prototype.isValid = function() {
    return null !== this.parsed;
  }, r2.prototype.brightness = function() {
    return n(H(this.rgba), 2);
  }, r2.prototype.isDark = function() {
    return H(this.rgba) < 0.5;
  }, r2.prototype.isLight = function() {
    return H(this.rgba) >= 0.5;
  }, r2.prototype.toHex = function() {
    return r3 = o(this.rgba), t2 = r3.r, e2 = r3.g, u2 = r3.b, i2 = (a3 = r3.a) < 1 ? s(n(255 * a3)) : "", "#" + s(t2) + s(e2) + s(u2) + i2;
    var r3, t2, e2, u2, a3, i2;
  }, r2.prototype.toRgb = function() {
    return o(this.rgba);
  }, r2.prototype.toRgbString = function() {
    return r3 = o(this.rgba), t2 = r3.r, n2 = r3.g, e2 = r3.b, (u2 = r3.a) < 1 ? "rgba(" + t2 + ", " + n2 + ", " + e2 + ", " + u2 + ")" : "rgb(" + t2 + ", " + n2 + ", " + e2 + ")";
    var r3, t2, n2, e2, u2;
  }, r2.prototype.toHsl = function() {
    return d(c(this.rgba));
  }, r2.prototype.toHslString = function() {
    return r3 = d(c(this.rgba)), t2 = r3.h, n2 = r3.s, e2 = r3.l, (u2 = r3.a) < 1 ? "hsla(" + t2 + ", " + n2 + "%, " + e2 + "%, " + u2 + ")" : "hsl(" + t2 + ", " + n2 + "%, " + e2 + "%)";
    var r3, t2, n2, e2, u2;
  }, r2.prototype.toHsv = function() {
    return r3 = h(this.rgba), { h: n(r3.h), s: n(r3.s), v: n(r3.v), a: n(r3.a, 3) };
    var r3;
  }, r2.prototype.invert = function() {
    return w({ r: 255 - (r3 = this.rgba).r, g: 255 - r3.g, b: 255 - r3.b, a: r3.a });
    var r3;
  }, r2.prototype.saturate = function(r3) {
    return void 0 === r3 && (r3 = 0.1), w(M2(this.rgba, r3));
  }, r2.prototype.desaturate = function(r3) {
    return void 0 === r3 && (r3 = 0.1), w(M2(this.rgba, -r3));
  }, r2.prototype.grayscale = function() {
    return w(M2(this.rgba, -1));
  }, r2.prototype.lighten = function(r3) {
    return void 0 === r3 && (r3 = 0.1), w($2(this.rgba, r3));
  }, r2.prototype.darken = function(r3) {
    return void 0 === r3 && (r3 = 0.1), w($2(this.rgba, -r3));
  }, r2.prototype.rotate = function(r3) {
    return void 0 === r3 && (r3 = 15), this.hue(this.hue() + r3);
  }, r2.prototype.alpha = function(r3) {
    return "number" == typeof r3 ? w({ r: (t2 = this.rgba).r, g: t2.g, b: t2.b, a: r3 }) : n(this.rgba.a, 3);
    var t2;
  }, r2.prototype.hue = function(r3) {
    var t2 = c(this.rgba);
    return "number" == typeof r3 ? w({ h: r3, s: t2.s, l: t2.l, a: t2.a }) : n(t2.h);
  }, r2.prototype.isEqual = function(r3) {
    return this.toHex() === w(r3).toHex();
  }, r2;
})();
var w = function(r2) {
  return r2 instanceof j ? r2 : new j(r2);
};

// node_modules/dequal/lite/index.mjs
var has = Object.prototype.hasOwnProperty;
function dequal(foo, bar) {
  var ctor, len;
  if (foo === bar) return true;
  if (foo && bar && (ctor = foo.constructor) === bar.constructor) {
    if (ctor === Date) return foo.getTime() === bar.getTime();
    if (ctor === RegExp) return foo.toString() === bar.toString();
    if (ctor === Array) {
      if ((len = foo.length) === bar.length) {
        while (len-- && dequal(foo[len], bar[len])) ;
      }
      return len === -1;
    }
    if (!ctor || typeof foo === "object") {
      len = 0;
      for (ctor in foo) {
        if (has.call(foo, ctor) && ++len && !has.call(bar, ctor)) return false;
        if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor])) return false;
      }
      return Object.keys(bar).length === len;
    }
  }
  return foo !== foo && bar !== bar;
}

// node_modules/quickselect/index.js
function quickselect(arr, k, left = 0, right = arr.length - 1, compare = defaultCompare) {
  while (right > left) {
    if (right - left > 600) {
      const n2 = right - left + 1;
      const m2 = k - left + 1;
      const z2 = Math.log(n2);
      const s2 = 0.5 * Math.exp(2 * z2 / 3);
      const sd = 0.5 * Math.sqrt(z2 * s2 * (n2 - s2) / n2) * (m2 - n2 / 2 < 0 ? -1 : 1);
      const newLeft = Math.max(left, Math.floor(k - m2 * s2 / n2 + sd));
      const newRight = Math.min(right, Math.floor(k + (n2 - m2) * s2 / n2 + sd));
      quickselect(arr, k, newLeft, newRight, compare);
    }
    const t2 = arr[k];
    let i2 = left;
    let j2 = right;
    swap(arr, left, k);
    if (compare(arr[right], t2) > 0) swap(arr, left, right);
    while (i2 < j2) {
      swap(arr, i2, j2);
      i2++;
      j2--;
      while (compare(arr[i2], t2) < 0) i2++;
      while (compare(arr[j2], t2) > 0) j2--;
    }
    if (compare(arr[left], t2) === 0) swap(arr, left, j2);
    else {
      j2++;
      swap(arr, j2, right);
    }
    if (j2 <= k) left = j2 + 1;
    if (k <= j2) right = j2 - 1;
  }
}
function swap(arr, i2, j2) {
  const tmp = arr[i2];
  arr[i2] = arr[j2];
  arr[j2] = tmp;
}
function defaultCompare(a3, b2) {
  return a3 < b2 ? -1 : a3 > b2 ? 1 : 0;
}

// node_modules/rbush/index.js
var RBush = class {
  constructor(maxEntries = 9) {
    this._maxEntries = Math.max(4, maxEntries);
    this._minEntries = Math.max(2, Math.ceil(this._maxEntries * 0.4));
    this.clear();
  }
  all() {
    return this._all(this.data, []);
  }
  search(bbox) {
    let node = this.data;
    const result = [];
    if (!intersects(bbox, node)) return result;
    const toBBox = this.toBBox;
    const nodesToSearch = [];
    while (node) {
      for (let i2 = 0; i2 < node.children.length; i2++) {
        const child = node.children[i2];
        const childBBox = node.leaf ? toBBox(child) : child;
        if (intersects(bbox, childBBox)) {
          if (node.leaf) result.push(child);
          else if (contains(bbox, childBBox)) this._all(child, result);
          else nodesToSearch.push(child);
        }
      }
      node = nodesToSearch.pop();
    }
    return result;
  }
  collides(bbox) {
    let node = this.data;
    if (!intersects(bbox, node)) return false;
    const nodesToSearch = [];
    while (node) {
      for (let i2 = 0; i2 < node.children.length; i2++) {
        const child = node.children[i2];
        const childBBox = node.leaf ? this.toBBox(child) : child;
        if (intersects(bbox, childBBox)) {
          if (node.leaf || contains(bbox, childBBox)) return true;
          nodesToSearch.push(child);
        }
      }
      node = nodesToSearch.pop();
    }
    return false;
  }
  load(data) {
    if (!(data && data.length)) return this;
    if (data.length < this._minEntries) {
      for (let i2 = 0; i2 < data.length; i2++) {
        this.insert(data[i2]);
      }
      return this;
    }
    let node = this._build(data.slice(), 0, data.length - 1, 0);
    if (!this.data.children.length) {
      this.data = node;
    } else if (this.data.height === node.height) {
      this._splitRoot(this.data, node);
    } else {
      if (this.data.height < node.height) {
        const tmpNode = this.data;
        this.data = node;
        node = tmpNode;
      }
      this._insert(node, this.data.height - node.height - 1, true);
    }
    return this;
  }
  insert(item) {
    if (item) this._insert(item, this.data.height - 1);
    return this;
  }
  clear() {
    this.data = createNode([]);
    return this;
  }
  remove(item, equalsFn) {
    if (!item) return this;
    let node = this.data;
    const bbox = this.toBBox(item);
    const path = [];
    const indexes = [];
    let i2, parent, goingUp;
    while (node || path.length) {
      if (!node) {
        node = path.pop();
        parent = path[path.length - 1];
        i2 = indexes.pop();
        goingUp = true;
      }
      if (node.leaf) {
        const index = findItem(item, node.children, equalsFn);
        if (index !== -1) {
          node.children.splice(index, 1);
          path.push(node);
          this._condense(path);
          return this;
        }
      }
      if (!goingUp && !node.leaf && contains(node, bbox)) {
        path.push(node);
        indexes.push(i2);
        i2 = 0;
        parent = node;
        node = node.children[0];
      } else if (parent) {
        i2++;
        node = parent.children[i2];
        goingUp = false;
      } else node = null;
    }
    return this;
  }
  toBBox(item) {
    return item;
  }
  compareMinX(a3, b2) {
    return a3.minX - b2.minX;
  }
  compareMinY(a3, b2) {
    return a3.minY - b2.minY;
  }
  toJSON() {
    return this.data;
  }
  fromJSON(data) {
    this.data = data;
    return this;
  }
  _all(node, result) {
    const nodesToSearch = [];
    while (node) {
      if (node.leaf) result.push(...node.children);
      else nodesToSearch.push(...node.children);
      node = nodesToSearch.pop();
    }
    return result;
  }
  _build(items, left, right, height) {
    const N3 = right - left + 1;
    let M3 = this._maxEntries;
    let node;
    if (N3 <= M3) {
      node = createNode(items.slice(left, right + 1));
      calcBBox(node, this.toBBox);
      return node;
    }
    if (!height) {
      height = Math.ceil(Math.log(N3) / Math.log(M3));
      M3 = Math.ceil(N3 / Math.pow(M3, height - 1));
    }
    node = createNode([]);
    node.leaf = false;
    node.height = height;
    const N22 = Math.ceil(N3 / M3);
    const N1 = N22 * Math.ceil(Math.sqrt(M3));
    multiSelect(items, left, right, N1, this.compareMinX);
    for (let i2 = left; i2 <= right; i2 += N1) {
      const right2 = Math.min(i2 + N1 - 1, right);
      multiSelect(items, i2, right2, N22, this.compareMinY);
      for (let j2 = i2; j2 <= right2; j2 += N22) {
        const right3 = Math.min(j2 + N22 - 1, right2);
        node.children.push(this._build(items, j2, right3, height - 1));
      }
    }
    calcBBox(node, this.toBBox);
    return node;
  }
  _chooseSubtree(bbox, node, level, path) {
    while (true) {
      path.push(node);
      if (node.leaf || path.length - 1 === level) break;
      let minArea = Infinity;
      let minEnlargement = Infinity;
      let targetNode;
      for (let i2 = 0; i2 < node.children.length; i2++) {
        const child = node.children[i2];
        const area = bboxArea(child);
        const enlargement = enlargedArea(bbox, child) - area;
        if (enlargement < minEnlargement) {
          minEnlargement = enlargement;
          minArea = area < minArea ? area : minArea;
          targetNode = child;
        } else if (enlargement === minEnlargement) {
          if (area < minArea) {
            minArea = area;
            targetNode = child;
          }
        }
      }
      node = targetNode || node.children[0];
    }
    return node;
  }
  _insert(item, level, isNode) {
    const bbox = isNode ? item : this.toBBox(item);
    const insertPath = [];
    const node = this._chooseSubtree(bbox, this.data, level, insertPath);
    node.children.push(item);
    extend(node, bbox);
    while (level >= 0) {
      if (insertPath[level].children.length > this._maxEntries) {
        this._split(insertPath, level);
        level--;
      } else break;
    }
    this._adjustParentBBoxes(bbox, insertPath, level);
  }
  // split overflowed node into two
  _split(insertPath, level) {
    const node = insertPath[level];
    const M3 = node.children.length;
    const m2 = this._minEntries;
    this._chooseSplitAxis(node, m2, M3);
    const splitIndex = this._chooseSplitIndex(node, m2, M3);
    const newNode = createNode(node.children.splice(splitIndex, node.children.length - splitIndex));
    newNode.height = node.height;
    newNode.leaf = node.leaf;
    calcBBox(node, this.toBBox);
    calcBBox(newNode, this.toBBox);
    if (level) insertPath[level - 1].children.push(newNode);
    else this._splitRoot(node, newNode);
  }
  _splitRoot(node, newNode) {
    this.data = createNode([node, newNode]);
    this.data.height = node.height + 1;
    this.data.leaf = false;
    calcBBox(this.data, this.toBBox);
  }
  _chooseSplitIndex(node, m2, M3) {
    let index;
    let minOverlap = Infinity;
    let minArea = Infinity;
    for (let i2 = m2; i2 <= M3 - m2; i2++) {
      const bbox1 = distBBox(node, 0, i2, this.toBBox);
      const bbox2 = distBBox(node, i2, M3, this.toBBox);
      const overlap = intersectionArea(bbox1, bbox2);
      const area = bboxArea(bbox1) + bboxArea(bbox2);
      if (overlap < minOverlap) {
        minOverlap = overlap;
        index = i2;
        minArea = area < minArea ? area : minArea;
      } else if (overlap === minOverlap) {
        if (area < minArea) {
          minArea = area;
          index = i2;
        }
      }
    }
    return index || M3 - m2;
  }
  // sorts node children by the best axis for split
  _chooseSplitAxis(node, m2, M3) {
    const compareMinX = node.leaf ? this.compareMinX : compareNodeMinX;
    const compareMinY = node.leaf ? this.compareMinY : compareNodeMinY;
    const xMargin = this._allDistMargin(node, m2, M3, compareMinX);
    const yMargin = this._allDistMargin(node, m2, M3, compareMinY);
    if (xMargin < yMargin) node.children.sort(compareMinX);
  }
  // total margin of all possible split distributions where each node is at least m full
  _allDistMargin(node, m2, M3, compare) {
    node.children.sort(compare);
    const toBBox = this.toBBox;
    const leftBBox = distBBox(node, 0, m2, toBBox);
    const rightBBox = distBBox(node, M3 - m2, M3, toBBox);
    let margin = bboxMargin(leftBBox) + bboxMargin(rightBBox);
    for (let i2 = m2; i2 < M3 - m2; i2++) {
      const child = node.children[i2];
      extend(leftBBox, node.leaf ? toBBox(child) : child);
      margin += bboxMargin(leftBBox);
    }
    for (let i2 = M3 - m2 - 1; i2 >= m2; i2--) {
      const child = node.children[i2];
      extend(rightBBox, node.leaf ? toBBox(child) : child);
      margin += bboxMargin(rightBBox);
    }
    return margin;
  }
  _adjustParentBBoxes(bbox, path, level) {
    for (let i2 = level; i2 >= 0; i2--) {
      extend(path[i2], bbox);
    }
  }
  _condense(path) {
    for (let i2 = path.length - 1, siblings; i2 >= 0; i2--) {
      if (path[i2].children.length === 0) {
        if (i2 > 0) {
          siblings = path[i2 - 1].children;
          siblings.splice(siblings.indexOf(path[i2]), 1);
        } else this.clear();
      } else calcBBox(path[i2], this.toBBox);
    }
  }
};
function findItem(item, items, equalsFn) {
  if (!equalsFn) return items.indexOf(item);
  for (let i2 = 0; i2 < items.length; i2++) {
    if (equalsFn(item, items[i2])) return i2;
  }
  return -1;
}
function calcBBox(node, toBBox) {
  distBBox(node, 0, node.children.length, toBBox, node);
}
function distBBox(node, k, p2, toBBox, destNode) {
  if (!destNode) destNode = createNode(null);
  destNode.minX = Infinity;
  destNode.minY = Infinity;
  destNode.maxX = -Infinity;
  destNode.maxY = -Infinity;
  for (let i2 = k; i2 < p2; i2++) {
    const child = node.children[i2];
    extend(destNode, node.leaf ? toBBox(child) : child);
  }
  return destNode;
}
function extend(a3, b2) {
  a3.minX = Math.min(a3.minX, b2.minX);
  a3.minY = Math.min(a3.minY, b2.minY);
  a3.maxX = Math.max(a3.maxX, b2.maxX);
  a3.maxY = Math.max(a3.maxY, b2.maxY);
  return a3;
}
function compareNodeMinX(a3, b2) {
  return a3.minX - b2.minX;
}
function compareNodeMinY(a3, b2) {
  return a3.minY - b2.minY;
}
function bboxArea(a3) {
  return (a3.maxX - a3.minX) * (a3.maxY - a3.minY);
}
function bboxMargin(a3) {
  return a3.maxX - a3.minX + (a3.maxY - a3.minY);
}
function enlargedArea(a3, b2) {
  return (Math.max(b2.maxX, a3.maxX) - Math.min(b2.minX, a3.minX)) * (Math.max(b2.maxY, a3.maxY) - Math.min(b2.minY, a3.minY));
}
function intersectionArea(a3, b2) {
  const minX = Math.max(a3.minX, b2.minX);
  const minY = Math.max(a3.minY, b2.minY);
  const maxX = Math.min(a3.maxX, b2.maxX);
  const maxY = Math.min(a3.maxY, b2.maxY);
  return Math.max(0, maxX - minX) * Math.max(0, maxY - minY);
}
function contains(a3, b2) {
  return a3.minX <= b2.minX && a3.minY <= b2.minY && b2.maxX <= a3.maxX && b2.maxY <= a3.maxY;
}
function intersects(a3, b2) {
  return b2.minX <= a3.maxX && b2.minY <= a3.maxY && b2.maxX >= a3.minX && b2.maxY >= a3.minY;
}
function createNode(children) {
  return {
    children,
    height: 1,
    leaf: true,
    minX: Infinity,
    minY: Infinity,
    maxX: -Infinity,
    maxY: -Infinity
  };
}
function multiSelect(arr, left, right, n2, compare) {
  const stack = [left, right];
  while (stack.length) {
    right = stack.pop();
    left = stack.pop();
    if (right - left <= n2) continue;
    const mid = left + Math.ceil((right - left) / n2 / 2) * n2;
    quickselect(arr, mid, left, right, compare);
    stack.push(left, mid, mid, right);
  }
}

// node_modules/debounce/index.js
function debounce(function_, wait = 100, options = {}) {
  if (typeof function_ !== "function") {
    throw new TypeError(`Expected the first parameter to be a function, got \`${typeof function_}\`.`);
  }
  if (wait < 0) {
    throw new RangeError("`wait` must not be negative.");
  }
  if (typeof options === "boolean") {
    throw new TypeError("The `options` parameter must be an object, not a boolean. Use `{immediate: true}` instead.");
  }
  const { immediate } = options;
  let storedContext;
  let storedArguments;
  let timeoutId;
  let timestamp;
  let result;
  function run() {
    const callContext = storedContext;
    const callArguments = storedArguments;
    storedContext = void 0;
    storedArguments = void 0;
    result = function_.apply(callContext, callArguments);
    return result;
  }
  function later() {
    const last = Date.now() - timestamp;
    if (last < wait && last >= 0) {
      timeoutId = setTimeout(later, wait - last);
    } else {
      timeoutId = void 0;
      if (!immediate) {
        result = run();
      }
    }
  }
  const debounced = function(...arguments_) {
    if (storedContext && this !== storedContext && Object.getPrototypeOf(this) === Object.getPrototypeOf(storedContext)) {
      throw new Error("Debounced method called with different contexts of the same prototype.");
    }
    storedContext = this;
    storedArguments = arguments_;
    timestamp = Date.now();
    const callNow = immediate && !timeoutId;
    if (!timeoutId) {
      timeoutId = setTimeout(later, wait);
    }
    if (callNow) {
      result = run();
      return result;
    }
    return void 0;
  };
  Object.defineProperty(debounced, "isPending", {
    get() {
      return timeoutId !== void 0;
    }
  });
  debounced.clear = () => {
    if (!timeoutId) {
      return;
    }
    clearTimeout(timeoutId);
    timeoutId = void 0;
    storedContext = void 0;
    storedArguments = void 0;
  };
  debounced.flush = () => {
    if (!timeoutId) {
      return;
    }
    debounced.trigger();
  };
  debounced.trigger = () => {
    result = run();
    debounced.clear();
  };
  return debounced;
}

// node_modules/hotkeys-js/dist/hotkeys-js.js
var isff = typeof navigator !== "undefined" ? navigator.userAgent.toLowerCase().indexOf("firefox") > 0 : false;
function addEvent(object, event, method, useCapture) {
  if (object.addEventListener) {
    object.addEventListener(event, method, useCapture);
  } else if (object.attachEvent) {
    object.attachEvent(`on${event}`, method);
  }
}
function removeEvent(object, event, method, useCapture) {
  if (!object) return;
  if (object.removeEventListener) {
    object.removeEventListener(event, method, useCapture);
  } else if (object.detachEvent) {
    object.detachEvent(`on${event}`, method);
  }
}
function getMods(modifier, key) {
  const modsKeys = key.slice(0, key.length - 1);
  const modsCodes = [];
  for (let i2 = 0; i2 < modsKeys.length; i2++) {
    modsCodes.push(modifier[modsKeys[i2].toLowerCase()]);
  }
  return modsCodes;
}
function getKeys(key) {
  if (typeof key !== "string") key = "";
  key = key.replace(/\s/g, "");
  const keys = key.split(",");
  let index = keys.lastIndexOf("");
  for (; index >= 0; ) {
    keys[index - 1] += ",";
    keys.splice(index, 1);
    index = keys.lastIndexOf("");
  }
  return keys;
}
function compareArray(a1, a22) {
  const arr1 = a1.length >= a22.length ? a1 : a22;
  const arr2 = a1.length >= a22.length ? a22 : a1;
  let isIndex = true;
  for (let i2 = 0; i2 < arr1.length; i2++) {
    if (arr2.indexOf(arr1[i2]) === -1) isIndex = false;
  }
  return isIndex;
}
function getLayoutIndependentKeyCode(event) {
  let key = event.keyCode || event.which || event.charCode;
  if (event.key && /^[a-z]$/i.test(event.key)) {
    return event.key.toUpperCase().charCodeAt(0);
  }
  if (event.code && /^Key[A-Z]$/.test(event.code)) {
    key = event.code.charCodeAt(3);
  }
  return key;
}
var _keyMap = {
  backspace: 8,
  "⌫": 8,
  tab: 9,
  clear: 12,
  enter: 13,
  "↩": 13,
  return: 13,
  esc: 27,
  escape: 27,
  space: 32,
  left: 37,
  up: 38,
  right: 39,
  down: 40,
  /// https://w3c.github.io/uievents/#events-keyboard-key-location
  arrowup: 38,
  arrowdown: 40,
  arrowleft: 37,
  arrowright: 39,
  del: 46,
  delete: 46,
  ins: 45,
  insert: 45,
  home: 36,
  end: 35,
  pageup: 33,
  pagedown: 34,
  capslock: 20,
  num_0: 96,
  num_1: 97,
  num_2: 98,
  num_3: 99,
  num_4: 100,
  num_5: 101,
  num_6: 102,
  num_7: 103,
  num_8: 104,
  num_9: 105,
  num_multiply: 106,
  num_add: 107,
  num_enter: 108,
  num_subtract: 109,
  num_decimal: 110,
  num_divide: 111,
  "⇪": 20,
  ",": 188,
  ".": 190,
  "/": 191,
  "`": 192,
  "-": isff ? 173 : 189,
  "=": isff ? 61 : 187,
  ";": isff ? 59 : 186,
  "'": 222,
  "{": 219,
  "}": 221,
  "[": 219,
  "]": 221,
  "\\": 220
};
var _modifier = {
  // shiftKey
  "⇧": 16,
  shift: 16,
  // altKey
  "⌥": 18,
  alt: 18,
  option: 18,
  // ctrlKey
  "⌃": 17,
  ctrl: 17,
  control: 17,
  // metaKey
  "⌘": 91,
  cmd: 91,
  meta: 91,
  command: 91
};
var modifierMap = {
  16: "shiftKey",
  18: "altKey",
  17: "ctrlKey",
  91: "metaKey",
  shiftKey: 16,
  ctrlKey: 17,
  altKey: 18,
  metaKey: 91
};
var _mods = {
  16: false,
  18: false,
  17: false,
  91: false
};
var _handlers = {};
for (let k = 1; k < 20; k++) {
  _keyMap[`f${k}`] = 111 + k;
}
var _downKeys = [];
var winListendFocus = null;
var winListendFullscreen = null;
var _scope = "all";
var elementEventMap = /* @__PURE__ */ new Map();
var code = (x2) => _keyMap[x2.toLowerCase()] || _modifier[x2.toLowerCase()] || x2.toUpperCase().charCodeAt(0);
var getKey = (x2) => Object.keys(_keyMap).find((k) => _keyMap[k] === x2);
var getModifier = (x2) => Object.keys(_modifier).find((k) => _modifier[k] === x2);
var setScope = (scope) => {
  _scope = scope || "all";
};
var getScope = () => {
  return _scope || "all";
};
var getPressedKeyCodes = () => {
  return _downKeys.slice(0);
};
var getPressedKeyString = () => {
  return _downKeys.map(
    (c2) => getKey(c2) || getModifier(c2) || String.fromCharCode(c2)
  );
};
var getAllKeyCodes = () => {
  const result = [];
  Object.keys(_handlers).forEach((k) => {
    _handlers[k].forEach(({ key, scope, mods, shortcut }) => {
      result.push({
        scope,
        shortcut,
        mods,
        keys: key.split("+").map((v2) => code(v2))
      });
    });
  });
  return result;
};
var filter = (event) => {
  const target = event.target || event.srcElement;
  const { tagName } = target;
  let flag = true;
  const isInput = tagName === "INPUT" && ![
    "checkbox",
    "radio",
    "range",
    "button",
    "file",
    "reset",
    "submit",
    "color"
  ].includes(target.type);
  if (target.isContentEditable || (isInput || tagName === "TEXTAREA" || tagName === "SELECT") && !target.readOnly) {
    flag = false;
  }
  return flag;
};
var isPressed = (keyCode) => {
  if (typeof keyCode === "string") {
    keyCode = code(keyCode);
  }
  return _downKeys.indexOf(keyCode) !== -1;
};
var deleteScope = (scope, newScope) => {
  let handlers;
  let i2;
  if (!scope) scope = getScope();
  for (const key in _handlers) {
    if (Object.prototype.hasOwnProperty.call(_handlers, key)) {
      handlers = _handlers[key];
      for (i2 = 0; i2 < handlers.length; ) {
        if (handlers[i2].scope === scope) {
          const deleteItems = handlers.splice(i2, 1);
          deleteItems.forEach(({ element }) => removeKeyEvent(element));
        } else {
          i2++;
        }
      }
    }
  }
  if (getScope() === scope) setScope(newScope || "all");
};
function clearModifier(event) {
  let key = getLayoutIndependentKeyCode(event);
  if (event.key && event.key.toLowerCase() === "capslock") {
    key = code(event.key);
  }
  const i2 = _downKeys.indexOf(key);
  if (i2 >= 0) {
    _downKeys.splice(i2, 1);
  }
  if (event.key && event.key.toLowerCase() === "meta") {
    _downKeys.splice(0, _downKeys.length);
  }
  if (key === 93 || key === 224) key = 91;
  if (key in _mods) {
    _mods[key] = false;
    for (const k in _modifier)
      if (_modifier[k] === key) hotkeys[k] = false;
  }
}
var unbind = (keysInfo, ...args) => {
  if (typeof keysInfo === "undefined") {
    Object.keys(_handlers).forEach((key) => {
      if (Array.isArray(_handlers[key])) {
        _handlers[key].forEach((info) => eachUnbind(info));
      }
      delete _handlers[key];
    });
    removeKeyEvent(null);
  } else if (Array.isArray(keysInfo)) {
    keysInfo.forEach((info) => {
      if (info.key) eachUnbind(info);
    });
  } else if (typeof keysInfo === "object") {
    if (keysInfo.key) eachUnbind(keysInfo);
  } else if (typeof keysInfo === "string") {
    let [scope, method] = args;
    if (typeof scope === "function") {
      method = scope;
      scope = "";
    }
    eachUnbind({
      key: keysInfo,
      scope,
      method,
      splitKey: "+"
    });
  }
};
var eachUnbind = ({
  key,
  scope,
  method,
  splitKey = "+"
}) => {
  const multipleKeys = getKeys(key);
  multipleKeys.forEach((originKey) => {
    const unbindKeys = originKey.split(splitKey);
    const len = unbindKeys.length;
    const lastKey = unbindKeys[len - 1];
    const keyCode = lastKey === "*" ? "*" : code(lastKey);
    if (!_handlers[keyCode]) return;
    if (!scope) scope = getScope();
    const mods = len > 1 ? getMods(_modifier, unbindKeys) : [];
    const unbindElements = [];
    _handlers[keyCode] = _handlers[keyCode].filter((record) => {
      const isMatchingMethod = method ? record.method === method : true;
      const isUnbind = isMatchingMethod && record.scope === scope && compareArray(record.mods, mods);
      if (isUnbind) unbindElements.push(record.element);
      return !isUnbind;
    });
    unbindElements.forEach((element) => removeKeyEvent(element));
  });
};
function eventHandler(event, handler, scope, element) {
  if (handler.element !== element) {
    return;
  }
  let modifiersMatch;
  if (handler.scope === scope || handler.scope === "all") {
    modifiersMatch = handler.mods.length > 0;
    for (const y3 in _mods) {
      if (Object.prototype.hasOwnProperty.call(_mods, y3)) {
        if (!_mods[y3] && handler.mods.indexOf(+y3) > -1 || _mods[y3] && handler.mods.indexOf(+y3) === -1) {
          modifiersMatch = false;
        }
      }
    }
    if (handler.mods.length === 0 && !_mods[16] && !_mods[18] && !_mods[17] && !_mods[91] || modifiersMatch || handler.shortcut === "*") {
      handler.keys = [];
      handler.keys = handler.keys.concat(_downKeys);
      if (handler.method(event, handler) === false) {
        if (event.preventDefault) event.preventDefault();
        else event.returnValue = false;
        if (event.stopPropagation) event.stopPropagation();
        if (event.cancelBubble) event.cancelBubble = true;
      }
    }
  }
}
function dispatch(event, element) {
  const asterisk = _handlers["*"];
  let key = getLayoutIndependentKeyCode(event);
  if (event.key && event.key.toLowerCase() === "capslock") {
    return;
  }
  const filterFn = hotkeys.filter || filter;
  if (!filterFn.call(this, event)) return;
  if (key === 93 || key === 224) key = 91;
  if (_downKeys.indexOf(key) === -1 && key !== 229) _downKeys.push(key);
  ["metaKey", "ctrlKey", "altKey", "shiftKey"].forEach((keyName) => {
    const keyNum = modifierMap[keyName];
    if (event[keyName] && _downKeys.indexOf(keyNum) === -1) {
      _downKeys.push(keyNum);
    } else if (!event[keyName] && _downKeys.indexOf(keyNum) > -1) {
      _downKeys.splice(_downKeys.indexOf(keyNum), 1);
    } else if (keyName === "metaKey" && event[keyName]) {
      _downKeys = _downKeys.filter((k) => k in modifierMap || k === key);
    }
  });
  if (key in _mods) {
    _mods[key] = true;
    for (const k in _modifier) {
      if (Object.prototype.hasOwnProperty.call(_modifier, k)) {
        const eventKey = modifierMap[_modifier[k]];
        hotkeys[k] = event[eventKey];
      }
    }
    if (!asterisk) return;
  }
  for (const e2 in _mods) {
    if (Object.prototype.hasOwnProperty.call(_mods, e2)) {
      _mods[e2] = event[modifierMap[e2]];
    }
  }
  if (event.getModifierState && !(event.altKey && !event.ctrlKey) && event.getModifierState("AltGraph")) {
    if (_downKeys.indexOf(17) === -1) {
      _downKeys.push(17);
    }
    if (_downKeys.indexOf(18) === -1) {
      _downKeys.push(18);
    }
    _mods[17] = true;
    _mods[18] = true;
  }
  const scope = getScope();
  if (asterisk) {
    for (let i2 = 0; i2 < asterisk.length; i2++) {
      if (asterisk[i2].scope === scope && (event.type === "keydown" && asterisk[i2].keydown || event.type === "keyup" && asterisk[i2].keyup)) {
        eventHandler(event, asterisk[i2], scope, element);
      }
    }
  }
  if (!(key in _handlers)) return;
  const handlerKey = _handlers[key];
  const keyLen = handlerKey.length;
  for (let i2 = 0; i2 < keyLen; i2++) {
    if (event.type === "keydown" && handlerKey[i2].keydown || event.type === "keyup" && handlerKey[i2].keyup) {
      if (handlerKey[i2].key) {
        const record = handlerKey[i2];
        const { splitKey } = record;
        const keyShortcut = record.key.split(splitKey);
        const _downKeysCurrent = [];
        for (let a3 = 0; a3 < keyShortcut.length; a3++) {
          _downKeysCurrent.push(code(keyShortcut[a3]));
        }
        if (_downKeysCurrent.sort().join("") === _downKeys.sort().join("")) {
          eventHandler(event, record, scope, element);
        }
      }
    }
  }
}
var hotkeys = function hotkeys2(key, option, method) {
  _downKeys = [];
  const keys = getKeys(key);
  let mods = [];
  let scope = "all";
  let element = document;
  let i2 = 0;
  let keyup = false;
  let keydown = true;
  let splitKey = "+";
  let capture = false;
  let single = false;
  if (method === void 0 && typeof option === "function") {
    method = option;
  }
  if (Object.prototype.toString.call(option) === "[object Object]") {
    const opts = option;
    if (opts.scope) scope = opts.scope;
    if (opts.element) element = opts.element;
    if (opts.keyup) keyup = opts.keyup;
    if (opts.keydown !== void 0) keydown = opts.keydown;
    if (opts.capture !== void 0) capture = opts.capture;
    if (typeof opts.splitKey === "string") splitKey = opts.splitKey;
    if (opts.single === true) single = true;
  }
  if (typeof option === "string") scope = option;
  if (single) unbind(key, scope);
  for (; i2 < keys.length; i2++) {
    const currentKey = keys[i2].split(splitKey);
    mods = [];
    if (currentKey.length > 1) mods = getMods(_modifier, currentKey);
    let finalKey = currentKey[currentKey.length - 1];
    finalKey = finalKey === "*" ? "*" : code(finalKey);
    if (!(finalKey in _handlers)) _handlers[finalKey] = [];
    _handlers[finalKey].push({
      keyup,
      keydown,
      scope,
      mods,
      shortcut: keys[i2],
      method,
      key: keys[i2],
      splitKey,
      element
    });
  }
  if (typeof element !== "undefined" && typeof window !== "undefined") {
    if (!elementEventMap.has(element)) {
      const keydownListener = (event = window.event) => dispatch(event, element);
      const keyupListenr = (event = window.event) => {
        dispatch(event, element);
        clearModifier(event);
      };
      elementEventMap.set(element, { keydownListener, keyupListenr, capture });
      addEvent(element, "keydown", keydownListener, capture);
      addEvent(element, "keyup", keyupListenr, capture);
    }
    if (!winListendFocus) {
      const listener = () => {
        _downKeys = [];
      };
      winListendFocus = { listener, capture };
      addEvent(window, "focus", listener, capture);
    }
    if (!winListendFullscreen && typeof document !== "undefined") {
      const onFullscreenChange = () => {
        _downKeys = [];
        for (const k in _mods) _mods[k] = false;
        for (const k in _modifier) hotkeys2[k] = false;
      };
      const fullscreenListener = onFullscreenChange;
      const webkitListener = onFullscreenChange;
      document.addEventListener("fullscreenchange", fullscreenListener);
      document.addEventListener("webkitfullscreenchange", webkitListener);
      winListendFullscreen = { fullscreen: fullscreenListener, webkit: webkitListener };
    }
  }
};
function trigger(shortcut, scope = "all") {
  Object.keys(_handlers).forEach((key) => {
    const dataList = _handlers[key].filter(
      (item) => item.scope === scope && item.shortcut === shortcut
    );
    dataList.forEach((data) => {
      if (data && data.method) {
        data.method({}, data);
      }
    });
  });
}
function removeKeyEvent(element) {
  const values = Object.values(_handlers).flat();
  const findindex = values.findIndex(({ element: el }) => el === element);
  if (findindex < 0 && element) {
    const { keydownListener, keyupListenr, capture } = elementEventMap.get(element) || {};
    if (keydownListener && keyupListenr) {
      removeEvent(element, "keyup", keyupListenr, capture);
      removeEvent(element, "keydown", keydownListener, capture);
      elementEventMap.delete(element);
    }
  }
  if (values.length <= 0 || elementEventMap.size <= 0) {
    const eventKeys = Array.from(elementEventMap.keys());
    eventKeys.forEach((el) => {
      const { keydownListener, keyupListenr, capture } = elementEventMap.get(el) || {};
      if (keydownListener && keyupListenr) {
        removeEvent(el, "keyup", keyupListenr, capture);
        removeEvent(el, "keydown", keydownListener, capture);
        elementEventMap.delete(el);
      }
    });
    elementEventMap.clear();
    Object.keys(_handlers).forEach((key) => delete _handlers[key]);
    if (winListendFocus) {
      const { listener, capture } = winListendFocus;
      removeEvent(window, "focus", listener, capture);
      winListendFocus = null;
    }
    if (winListendFullscreen && typeof document !== "undefined") {
      document.removeEventListener("fullscreenchange", winListendFullscreen.fullscreen);
      document.removeEventListener("webkitfullscreenchange", winListendFullscreen.webkit);
      winListendFullscreen = null;
    }
  }
}
var _api = {
  getPressedKeyString,
  setScope,
  getScope,
  deleteScope,
  getPressedKeyCodes,
  getAllKeyCodes,
  isPressed,
  filter,
  trigger,
  unbind,
  keyMap: _keyMap,
  modifier: _modifier,
  modifierMap
};
for (const a3 in _api) {
  const key = a3;
  if (Object.prototype.hasOwnProperty.call(_api, key)) {
    hotkeys[key] = _api[key];
  }
}
if (typeof window !== "undefined") {
  const _hotkeys = window.hotkeys;
  hotkeys.noConflict = (deep) => {
    if (deep && window.hotkeys === hotkeys) {
      window.hotkeys = _hotkeys;
    }
    return hotkeys;
  };
  window.hotkeys = hotkeys;
}

// node_modules/poll/dist/poll.js
async function a2(a3, t2, e2 = (() => false)) {
  do {
    if (await a3(), await e2()) break;
    const i2 = "number" == typeof t2 ? t2 : t2();
    await new Promise(((a4) => setTimeout(a4, Math.max(0, i2))));
  } while (!await e2());
}

// node_modules/@recogito/text-annotator/dist/text-annotator.es.js
var xt = (e2) => e2.target.selector.every(P2);
var Lt = (e2) => e2.selector.every(P2);
function P2(e2) {
  return Array.isArray(e2) ? e2.every((t2) => "range" in t2 && t2.range instanceof Range && !t2.range.collapsed) : "range" in e2 && e2.range instanceof Range && !e2.range.collapsed;
}
var fe2 = typeof navigator < "u" && navigator.platform.startsWith("Mac");
var ge2 = "not-annotatable";
var K2 = `.${ge2}`;
var J2 = (e2, t2) => e2.contains(t2) ? !!(t2 instanceof HTMLElement ? t2.closest(K2) : t2.parentElement?.closest(K2)) : true;
var me2 = (e2, t2) => {
  const o2 = t2.commonAncestorContainer;
  return !J2(e2, o2);
};
var pe2 = /^\s*$/;
var He2 = (e2) => pe2.test(e2.toString());
var Nt = (e2) => pe2.test(e2.textContent || "");
var Pe2 = (e2) => {
  if (e2.length === 0) return [];
  if (e2.length === 1) return [e2[0]];
  e2.sort((s2, n2) => {
    const r2 = s2.compareBoundaryPoints(Range.START_TO_START, n2);
    return r2 !== 0 ? r2 : s2.compareBoundaryPoints(Range.END_TO_END, n2);
  });
  const [t2, ...o2] = e2, i2 = o2.reduce((s2, n2) => s2.current.compareBoundaryPoints(Range.START_TO_END, n2) >= 0 ? (s2.current.compareBoundaryPoints(Range.END_TO_END, n2) < 0 && s2.current.setEnd(n2.endContainer, n2.endOffset), s2) : (s2.merged.push(s2.current), s2.current = n2.cloneRange(), s2), { merged: [], current: t2.cloneRange() });
  return [...i2.merged, i2.current];
};
var Ue2 = (e2) => {
  (!e2.hasAttribute("tabindex") || e2.tabIndex < -1) && e2.setAttribute("tabindex", "-1"), e2.classList.add("no-focus-outline");
};
var Fe = function* (e2) {
  const t2 = document.createTreeWalker(
    e2.commonAncestorContainer,
    NodeFilter.SHOW_ELEMENT,
    (i2) => i2 instanceof HTMLElement && i2.classList.contains(ge2) && !i2.parentElement?.closest(K2) && e2.intersectsNode(i2) ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP
  );
  let o2;
  for (; o2 = t2.nextNode(); )
    o2 instanceof HTMLElement && (yield o2);
};
var Ke = (e2, t2) => {
  if (!me2(e2, t2)) return [];
  const o2 = [];
  let i2 = null;
  for (const s2 of Fe(t2)) {
    let n2;
    i2 ? (n2 = document.createRange(), n2.setStartAfter(i2), n2.setEndBefore(s2)) : (n2 = t2.cloneRange(), n2.setEndBefore(s2)), n2.collapsed || o2.push(n2), i2 = s2;
  }
  if (i2) {
    const s2 = t2.cloneRange();
    s2.setStartAfter(i2), s2.collapsed || o2.push(s2);
  }
  return o2.length > 0 ? o2 : [t2];
};
var se2 = (e2) => {
  const t2 = e2.cloneContents();
  return t2.querySelectorAll(K2).forEach((o2) => o2.remove()), t2;
};
var We = (e2, t2) => {
  const o2 = e2.cloneRange();
  return t2.contains(o2.startContainer) || o2.setStart(t2, 0), t2.contains(o2.endContainer) || o2.setEnd(t2, t2.childNodes.length), o2;
};
var ie2 = new Intl.Segmenter(void 0, { granularity: "grapheme" });
var $e = (e2, t2, o2 = 10, i2) => {
  const s2 = i2 ? e2.startContainer.parentElement.closest(i2) : t2, n2 = document.createRange();
  n2.setStart(s2, 0), n2.setEnd(e2.startContainer, e2.startOffset);
  const r2 = se2(n2).textContent, c2 = document.createRange();
  c2.setStart(e2.endContainer, e2.endOffset), s2 === document.body ? c2.setEnd(s2, s2.childNodes.length) : c2.setEndAfter(s2);
  const l2 = se2(c2).textContent, y3 = [...ie2.segment(r2)], m2 = [...ie2.segment(l2)];
  return {
    prefix: y3.slice(-o2).map((E) => E.segment).join(""),
    suffix: m2.slice(0, o2).map((E) => E.segment).join("")
  };
};
var Ye2 = (e2, t2, o2) => {
  const i2 = document.createRange(), s2 = o2 ? e2.startContainer.parentElement.closest(o2) : t2;
  i2.setStart(s2, 0), i2.setEnd(e2.startContainer, e2.startOffset);
  const n2 = se2(i2).textContent, r2 = e2.toString(), c2 = n2.length || 0, l2 = c2 + r2.length;
  return o2 ? { quote: r2, start: c2, end: l2, range: e2, offsetReference: s2 } : { quote: r2, start: c2, end: l2, range: e2 };
};
var Xe = (e2, t2) => {
  if (P2(e2)) return e2;
  const { start: o2, end: i2 } = e2, s2 = e2.offsetReference || t2, n2 = document.createTreeWalker(
    t2,
    NodeFilter.SHOW_TEXT,
    (m2) => m2.parentElement?.closest(K2) ? NodeFilter.FILTER_SKIP : NodeFilter.FILTER_ACCEPT
  );
  let r2 = 0;
  const c2 = document.createRange();
  let l2 = n2.nextNode();
  l2 === null && console.error("Could not revive annotation target. Content missing.");
  let y3 = !s2;
  for (; l2 !== null; ) {
    if (y3 ||= typeof s2?.contains == "function" ? s2.contains(l2) : false, y3) {
      const m2 = l2.textContent?.length || 0;
      if (r2 + m2 > o2) {
        c2.setStart(l2, o2 - r2);
        break;
      }
      r2 += m2;
    }
    l2 = n2.nextNode();
  }
  for (; l2 !== null; ) {
    const m2 = l2.textContent?.length || 0;
    if (r2 + m2 >= i2) {
      c2.setEnd(l2, i2 - r2);
      break;
    }
    r2 += m2, l2 = n2.nextNode();
  }
  return {
    ...e2,
    range: c2
  };
};
var ee2 = (e2, t2, o2 = (i2, s2) => Xe(i2, s2)) => ({
  ...e2,
  selector: e2.selector.flatMap((i2) => o2(i2, t2))
});
var Q2 = (e2, t2, o2) => ({
  ...e2,
  target: ee2(e2.target, t2, o2)
});
var he2 = (e2) => {
  if (!e2)
    return document.scrollingElement;
  const { overflowY: t2 } = window.getComputedStyle(e2);
  return t2 !== "visible" && t2 !== "hidden" && e2.scrollHeight > e2.clientHeight ? e2 : he2(e2.parentElement);
};
var ae2 = (e2, t2, o2) => {
  const i2 = o2.getBoundingClientRect(), s2 = o2.clientHeight, n2 = o2.clientWidth, r2 = t2.selector[0].range.getBoundingClientRect(), { width: c2, height: l2 } = e2.getAnnotationBounds(t2.annotation), y3 = r2.top - i2.top, m2 = r2.left - i2.left, E = o2.parentElement ? o2.scrollTop : 0, h2 = o2.parentElement ? o2.scrollLeft : 0, A = y3 + E - (s2 - l2) / 2, u2 = m2 + h2 - (n2 - c2) / 2;
  o2.scroll({ top: A, left: u2, behavior: "smooth" });
};
var Ve = (e2, t2) => (o2, i2) => {
  const s2 = typeof o2 == "string" ? o2 : o2.id, n2 = i2 ? typeof i2 == "string" ? document.getElementById(i2) : i2 : he2(e2);
  if (!n2)
    return console.warn(`The scroll parent is missing for the annotation: ${s2}`, { container: e2 }), false;
  const r2 = t2.getAnnotation(s2);
  if (!r2)
    return console.warn(`The annotation is missing in the store: ${s2}`), false;
  const { range: c2 } = r2.target.selector[0];
  if (c2 && !c2.collapsed)
    return ae2(t2, r2.target, n2), true;
  const l2 = ee2(r2.target, e2), { range: y3 } = l2.selector[0];
  return y3 && !y3.collapsed ? (ae2(t2, l2, n2), true) : false;
};
var Ot = (e2, t2) => ({
  parse: (o2) => je2(o2),
  serialize: (o2) => Ge2(o2, e2, t2)
});
var ze2 = (e2) => e2.quote !== void 0 && e2.start !== void 0 && e2.end !== void 0;
var qe = (e2) => {
  const {
    id: t2,
    creator: o2,
    created: i2,
    modified: s2,
    target: n2
  } = e2, r2 = Array.isArray(n2) ? n2 : [n2];
  if (r2.length === 0)
    return { error: Error(`No targets found for annotation: ${e2.id}`) };
  const c2 = {
    creator: Ue(o2),
    created: i2 ? new Date(i2) : void 0,
    updated: s2 ? new Date(s2) : void 0,
    annotation: t2,
    selector: [],
    // @ts-expect-error: `styleClass` is not part of the core `TextAnnotationTarget` type
    styleClass: "styleClass" in r2[0] ? r2[0].styleClass : void 0
  };
  for (const l2 of r2) {
    const m2 = (Array.isArray(l2.selector) ? l2.selector : [l2.selector]).reduce((E, h2) => {
      switch (h2.type) {
        case "TextQuoteSelector":
          E.quote = h2.exact;
          break;
        case "TextPositionSelector":
          E.start = h2.start, E.end = h2.end;
          break;
      }
      return E;
    }, {});
    if (ze2(m2))
      c2.selector.push(
        {
          ...m2,
          id: l2.id,
          // @ts-expect-error: `scope` is not part of the core `TextSelector` type
          scope: l2.scope
        }
      );
    else {
      const E = [
        m2.start ? void 0 : "TextPositionSelector",
        m2.quote ? void 0 : "TextQuoteSelector"
      ].filter(Boolean);
      return { error: Error(`Missing selector types: ${E.join(" and ")} for annotation: ${e2.id}`) };
    }
  }
  return { parsed: c2 };
};
var je2 = (e2) => {
  const t2 = e2.id || v4_default(), {
    creator: o2,
    created: i2,
    modified: s2,
    body: n2,
    ...r2
  } = e2, c2 = Ge(n2, t2), l2 = qe(e2);
  return "error" in l2 ? { error: l2.error } : {
    parsed: {
      ...r2,
      id: t2,
      bodies: c2,
      target: l2.parsed
    }
  };
};
var Ge2 = (e2, t2, o2) => {
  const { bodies: i2, target: s2, ...n2 } = e2, {
    selector: r2,
    creator: c2,
    created: l2,
    updated: y3,
    ...m2
  } = s2, E = r2.map((h2) => {
    const { id: A, quote: u2 } = h2, R = {
      type: "TextQuoteSelector",
      exact: u2
    };
    if (o2 && "range" in h2) {
      const { prefix: x2, suffix: _2 } = $e(h2.range, o2);
      R.prefix = x2, R.suffix = _2;
    }
    if (!("start" in h2))
      throw new Error("W3C serialization requires TextSelector with start/end");
    const { start: w2, end: T } = h2, N3 = {
      type: "TextPositionSelector",
      start: w2,
      end: T
    };
    return {
      ...m2,
      id: A,
      // @ts-expect-error: `scope` is not part of the core `TextSelector` type
      scope: "scope" in h2 ? h2.scope : void 0,
      source: t2,
      selector: [R, N3]
    };
  });
  return {
    ...n2,
    "@context": "http://www.w3.org/ns/anno.jsonld",
    id: e2.id,
    type: "Annotation",
    body: He(e2.bodies),
    creator: c2,
    created: l2?.toISOString(),
    modified: y3?.toISOString(),
    target: E
  };
};
var Qe2 = (e2) => e2.addEventListener("click", (t2) => {
  !t2.target.closest(K2) && !t2.target.closest("a") && t2.preventDefault();
});
var te2 = (e2) => ({
  ...e2,
  type: e2.type,
  x: e2.x,
  y: e2.y,
  clientX: e2.clientX,
  clientY: e2.clientY,
  offsetX: e2.offsetX,
  offsetY: e2.offsetY,
  screenX: e2.screenX,
  screenY: e2.screenY,
  isPrimary: e2.isPrimary,
  altKey: e2.altKey,
  ctrlKey: e2.ctrlKey,
  metaKey: e2.metaKey,
  shiftKey: e2.shiftKey,
  button: e2.button,
  buttons: e2.buttons,
  currentTarget: e2.currentTarget,
  target: e2.target,
  defaultPrevented: e2.defaultPrevented,
  detail: e2.detail,
  eventPhase: e2.eventPhase,
  pointerId: e2.pointerId,
  pointerType: e2.pointerType,
  timeStamp: e2.timeStamp
});
var Z = (e2) => ({
  ...e2,
  type: e2.type,
  key: e2.key,
  code: e2.code,
  location: e2.location,
  repeat: e2.repeat,
  altKey: e2.altKey,
  ctrlKey: e2.ctrlKey,
  metaKey: e2.metaKey,
  shiftKey: e2.shiftKey,
  currentTarget: e2.currentTarget,
  target: e2.target,
  defaultPrevented: e2.defaultPrevented,
  detail: e2.detail,
  timeStamp: e2.timeStamp
});
var ne2 = (e2, t2 = 10) => {
  let o2;
  return ((...i2) => {
    clearTimeout(o2), o2 = setTimeout(() => e2.apply(void 0, i2), t2);
  });
};
var ye = (e2, t2, o2, i2) => {
  const { store: s2, selection: n2, hover: r2 } = o2, c2 = createNanoEvents();
  let l2;
  const y3 = /* @__PURE__ */ new Map();
  let m2;
  const E = ct(i2), h2 = (S) => {
    const { x: b2, y: L } = t2.getBoundingClientRect(), M3 = s2.getAt(S.clientX - b2, S.clientY - L, false, m2);
    M3 && o2.selection.evalSelectAction(M3) !== ne.NONE ? r2.current !== M3.id && (t2.classList.add("hovered"), r2.set(M3.id)) : r2.current && (t2.classList.remove("hovered"), r2.set(null));
  };
  t2.addEventListener("pointermove", h2);
  const A = ne2((S = false) => requestAnimationFrame(() => {
    const b2 = at(t2), { minX: L, minY: M3, maxX: V2, maxY: z2 } = b2, q2 = m2 ? s2.getIntersecting(L, M3, V2, z2).filter(({ annotation: f3 }) => m2?.(f3)) : s2.getIntersecting(L, M3, V2, z2), W2 = n2.selected.map(({ id: f3 }) => f3), $3 = q2.map(({ annotation: f3, rects: v2 }) => {
      const k = W2.includes(f3.id), I2 = f3.id === r2.current;
      return {
        annotation: f3,
        rects: v2,
        state: { selected: k, hovered: I2 }
      };
    });
    e2.redraw($3, b2, l2, y3, S), setTimeout(() => {
      E(q2.map(({ annotation: f3 }) => f3)), c2.emit("onRedraw");
    }, 1);
  }), 10), u2 = (S, b2) => {
    b2 ? S ? y3.set(b2, S) : y3.delete(b2) : l2 = S, A(true);
  }, R = (S) => {
    m2 = S, A(false);
  }, w2 = (S, b2) => c2.on(S, b2), T = () => {
    A();
  };
  s2.observe(T);
  const N3 = n2.subscribe(() => {
    A();
  }), x2 = r2.subscribe(() => {
    A();
  }), _2 = () => {
    A(true);
  };
  document.addEventListener("scroll", _2, { capture: true, passive: true });
  const a3 = ne2(() => {
    s2.recalculatePositions(), A();
  }, 10);
  window.addEventListener("resize", a3);
  const d2 = new ResizeObserver(a3);
  d2.observe(t2);
  const p2 = { attributes: true, childList: true, subtree: true }, g2 = new MutationObserver(ne2((S) => {
    S.every((L) => L.target === t2 || t2.contains(L.target)) || A(true);
  }, 150));
  return g2.observe(document.body, p2), {
    destroy: () => {
      t2.removeEventListener("pointermove", h2), e2.destroy(), s2.unobserve(T), N3(), x2(), document.removeEventListener("scroll", _2), window.removeEventListener("resize", a3), d2.disconnect(), g2.disconnect();
    },
    on: w2,
    redraw: A,
    setStyle: u2,
    setFilter: R,
    setVisible: e2.setVisible.bind(e2)
  };
};
var U = {
  fill: "rgb(0, 128, 255)",
  fillOpacity: 0.18
};
var re2 = {
  fill: "rgb(0, 128, 255)",
  fillOpacity: 0.45
};
var Ze = (e2) => e2?.fillOpacity !== void 0 ? w(e2?.fill || U.fill).alpha(e2.fillOpacity).toHex() : e2?.fill ? e2.fill : w(U.fill).alpha(U.fillOpacity).toHex();
var Se = (e2, t2, o2) => t2 ? typeof t2 == "function" ? t2(e2.annotation, e2.state, o2) || (e2.state?.selected ? re2 : U) : t2 : e2.state?.selected ? re2 : U;
var Je = (e2, t2) => {
  const o2 = (n2, r2) => n2.x <= r2.x + r2.width && n2.x + n2.width >= r2.x && n2.y <= r2.y + r2.height && n2.y + n2.height >= r2.y, i2 = (n2) => n2.rects.reduce((r2, c2) => r2 + c2.width, 0), s2 = t2.filter(({ rects: n2 }) => n2.some((r2) => o2(e2, r2)));
  return s2.sort((n2, r2) => i2(r2) - i2(n2)), s2.findIndex((n2) => n2.rects.includes(e2));
};
var et = (e2) => {
  const t2 = [], o2 = document.createTreeWalker(
    e2.commonAncestorContainer,
    NodeFilter.SHOW_TEXT
  );
  e2.startContainer.nodeType === Node.TEXT_NODE && (o2.currentNode = e2.startContainer, e2.intersectsNode(e2.startContainer) && t2.push(e2.startContainer));
  let i2 = t2.length > 0, s2;
  for (; s2 = o2.nextNode(); )
    if (e2.intersectsNode(s2))
      i2 = true, t2.push(s2);
    else if (i2)
      break;
  if (t2.length < 2)
    return Array.from(e2.getClientRects());
  {
    const n2 = t2[0], r2 = t2[t2.length - 1], c2 = document.createRange();
    if (c2.selectNode(n2), e2.startContainer.nodeType === Node.TEXT_NODE)
      c2.setStart(n2, e2.startOffset);
    else {
      const m2 = e2.startContainer.childNodes[e2.startOffset];
      m2 ? c2.setStartBefore(m2) : c2.setStartAfter(e2.startContainer);
    }
    const l2 = document.createRange();
    if (l2.selectNode(r2), e2.endContainer.nodeType === Node.TEXT_NODE)
      l2.setEnd(r2, e2.endOffset);
    else {
      const m2 = e2.endContainer.childNodes[e2.endOffset];
      m2 ? l2.setEndBefore(m2) : l2.setEndAfter(e2.endContainer);
    }
    const y3 = (m2) => {
      const E = document.createRange();
      return E.selectNode(m2), Array.from(E.getClientRects());
    };
    return [
      ...Array.from(c2.getClientRects()),
      ...t2.slice(1, -1).flatMap(y3),
      ...Array.from(l2.getClientRects())
    ];
  }
};
var tt = (e2, t2, o2, i2) => {
  const s2 = (c2) => Math.round(c2 * 10) / 10, n2 = {
    top: s2(e2.top),
    bottom: s2(e2.bottom),
    left: s2(e2.left),
    right: s2(e2.right)
  }, r2 = {
    top: s2(t2.top),
    bottom: s2(t2.bottom),
    left: s2(t2.left),
    right: s2(t2.right)
  };
  if (Math.abs(n2.top - r2.top) < i2 && Math.abs(n2.bottom - r2.bottom) < i2) {
    if (Math.abs(n2.left - r2.right) < o2 || Math.abs(n2.right - r2.left) < o2)
      return "inline-adjacent";
    if (n2.left >= r2.left && n2.right <= r2.right)
      return "inline-is-contained";
    if (n2.left <= r2.left && n2.right >= r2.right)
      return "inline-contains";
  } else if (n2.top <= r2.top && n2.bottom >= r2.bottom) {
    if (n2.left <= r2.left && n2.right >= r2.right)
      return "block-contains";
  } else if (n2.top >= r2.top && n2.bottom <= r2.bottom && n2.left >= r2.left && n2.right <= r2.right)
    return "block-is-contained";
};
var nt = (e2, t2) => {
  const o2 = Math.min(e2.left, t2.left), i2 = Math.max(e2.right, t2.right), s2 = Math.min(e2.top, t2.top), n2 = Math.max(e2.bottom, t2.bottom);
  return new DOMRect(o2, s2, i2 - o2, n2 - s2);
};
var ot = (e2, t2 = 0.5, o2 = 0.5) => e2.reduce((i2, s2) => {
  if (s2.width === 0 || s2.height === 0)
    return i2;
  let n2 = [...i2], r2 = false;
  for (const c2 of i2) {
    const l2 = tt(s2, c2, t2, o2);
    if (l2 === "inline-adjacent") {
      n2 = n2.map((y3) => y3 === c2 ? nt(s2, c2) : y3), r2 = true;
      break;
    } else if (l2 === "inline-contains") {
      n2 = n2.map((y3) => y3 === c2 ? s2 : y3), r2 = true;
      break;
    } else if (l2 === "inline-is-contained") {
      r2 = true;
      break;
    } else if (l2 === "block-contains" || l2 === "block-is-contained") {
      s2.width < c2.width && (n2 = n2.map((y3) => y3 === c2 ? s2 : y3)), s2.width === c2.width && s2.height < c2.width && (n2 = n2.map((y3) => y3 === c2 ? s2 : y3)), r2 = true;
      break;
    }
  }
  return r2 ? n2 : [...n2, s2];
}, []);
var kt = (e2) => ({
  length: e2.length,
  item: (t2) => e2[t2],
  [Symbol.iterator]: function* () {
    for (let t2 = 0; t2 < this.length; t2++)
      yield this.item(t2);
  }
});
var st = (e2, t2) => {
  const { left: o2, top: i2, right: s2, bottom: n2 } = e2;
  return new DOMRect(o2 - t2.left, i2 - t2.top, s2 - o2, n2 - i2);
};
var Mt = (e2, t2) => {
  const { left: o2, top: i2, right: s2, bottom: n2 } = e2;
  return new DOMRect(o2 + t2.left, i2 + t2.top, s2 - o2, n2 - i2);
};
var rt = (e2) => {
  e2.classList.add("r6o-annotatable");
  const t2 = document.createElement("div");
  t2.className = "r6o-span-highlight-layer", e2.appendChild(t2);
  let o2 = [];
  return {
    destroy: () => {
      t2.remove();
    },
    redraw: (r2, c2, l2, y3, m2) => {
      if (!(!dequal(o2, r2) || m2)) return;
      t2.innerHTML = "", [...r2].sort((u2, R) => {
        const { annotation: { target: { created: w2 } } } = u2, { annotation: { target: { created: T } } } = R;
        return w2 && T ? w2.getTime() - T.getTime() : 0;
      }).forEach((u2) => {
        const R = y3?.get(u2.annotation.id) || l2;
        u2.rects.map((w2) => {
          const T = Je(w2, r2), N3 = Se(u2, R, T), x2 = document.createElement("span");
          x2.className = "r6o-annotation", x2.dataset.annotation = u2.annotation.id, x2.style.left = `${w2.x}px`, x2.style.top = `${w2.y}px`, x2.style.width = `${w2.width}px`, x2.style.height = `${w2.height}px`, x2.style.backgroundColor = Ze(N3), N3.underlineStyle && (x2.style.borderStyle = N3.underlineStyle), N3.underlineColor && (x2.style.borderColor = N3.underlineColor), N3.underlineThickness && (x2.style.borderBottomWidth = `${N3.underlineThickness}px`), N3.underlineOffset && (x2.style.paddingBottom = `${N3.underlineOffset}px`), t2.appendChild(x2);
        });
      }), o2 = r2;
    },
    setVisible: (r2) => {
      r2 ? t2.classList.remove("hidden") : t2.classList.add("hidden");
    }
  };
};
var it = (e2, t2, o2) => ye(rt(e2), e2, t2, o2);
var at = (e2) => {
  const { top: t2, left: o2 } = e2.getBoundingClientRect(), { innerWidth: i2, innerHeight: s2 } = window, n2 = -o2, r2 = -t2, c2 = i2 - o2, l2 = s2 - t2;
  return { top: t2, left: o2, minX: n2, minY: r2, maxX: c2, maxY: l2 };
};
var ct = (e2) => {
  let t2 = /* @__PURE__ */ new Set();
  return (i2) => {
    const s2 = i2.map((n2) => n2.id);
    (t2.size !== s2.length || s2.some((n2) => !t2.has(n2))) && e2.set(s2), t2 = new Set(s2);
  };
};
var lt = (e2, t2, o2, i2) => {
  const s2 = new RBush(), n2 = /* @__PURE__ */ new Map(), r2 = createNanoEvents(), c2 = (a3, d2) => {
    const p2 = a3.selector.flatMap((C) => et(C.range)), g2 = ot(p2, o2, i2).map((C) => st(C, d2));
    return g2.map((C) => {
      const { x: S, y: b2, width: L, height: M3 } = C;
      return {
        minX: S,
        minY: b2,
        maxX: S + L,
        maxY: b2 + M3,
        annotation: {
          id: a3.annotation,
          rects: g2
        }
      };
    });
  }, l2 = () => [...n2.values()], y3 = () => {
    s2.clear(), n2.clear();
  }, m2 = (a3) => {
    const d2 = c2(a3, t2.getBoundingClientRect());
    d2.length !== 0 && (d2.forEach((p2) => s2.insert(p2)), n2.set(a3.annotation, d2));
  }, E = (a3) => {
    const d2 = n2.get(a3.annotation);
    d2 && (d2.forEach((p2) => s2.remove(p2)), n2.delete(a3.annotation));
  }, h2 = (a3) => {
    E(a3), m2(a3);
  }, A = (a3, d2 = true) => {
    d2 && y3();
    const p2 = t2.getBoundingClientRect(), g2 = a3.map((S) => ({ target: S, rects: c2(S, p2) }));
    g2.forEach(({ target: S, rects: b2 }) => {
      b2.length > 0 && n2.set(S.annotation, b2);
    });
    const C = g2.flatMap(({ rects: S }) => S);
    s2.load(C);
  }, u2 = (a3, d2, p2 = false) => {
    const g2 = s2.search({
      minX: a3,
      minY: d2,
      maxX: a3,
      maxY: d2
    }), C = (S) => S.annotation.rects.reduce((b2, L) => b2 + L.width * L.height, 0);
    return g2.length > 0 ? (g2.sort((S, b2) => C(S) - C(b2)), p2 ? g2.map((S) => S.annotation.id) : [g2[0].annotation.id]) : [];
  }, R = (a3) => {
    const d2 = w2(a3);
    if (d2.length === 0)
      return;
    let p2 = d2[0].left, g2 = d2[0].top, C = d2[0].right, S = d2[0].bottom;
    for (let b2 = 1; b2 < d2.length; b2++) {
      const L = d2[b2];
      p2 = Math.min(p2, L.left), g2 = Math.min(g2, L.top), C = Math.max(C, L.right), S = Math.max(S, L.bottom);
    }
    return new DOMRect(p2, g2, C - p2, S - g2);
  }, w2 = (a3) => {
    const d2 = n2.get(a3);
    return d2 ? d2[0].annotation.rects : [];
  };
  return {
    all: l2,
    clear: y3,
    getAt: u2,
    getAnnotationBounds: R,
    getAnnotationRects: w2,
    getIntersecting: (a3, d2, p2, g2) => {
      const C = s2.search({ minX: a3, minY: d2, maxX: p2, maxY: g2 }), S = new Set(C.map((b2) => b2.annotation.id));
      return Array.from(S).map((b2) => ({
        annotation: e2.getAnnotation(b2),
        rects: w2(b2)
      })).filter((b2) => !!b2.annotation);
    },
    insert: m2,
    recalculate: () => {
      A(e2.all().map((a3) => a3.target), true), r2.emit("recalculate");
    },
    remove: E,
    set: A,
    size: () => s2.all().length,
    update: h2,
    on: (a3, d2) => r2.on(a3, d2)
  };
};
var dt = (e2, t2) => {
  const o2 = _e(), i2 = lt(
    o2,
    e2,
    t2.mergeHighlights?.horizontalTolerance,
    t2.mergeHighlights?.verticalTolerance
  ), s2 = Ne(o2, t2.userSelectAction, t2.adapter), n2 = De(o2), r2 = Ye(), c2 = (a3) => {
    const d2 = a3.map((g2) => Q2(g2, e2, t2.selectorReviveFn)), p2 = d2.filter((g2) => !P2(g2.target.selector));
    return { revived: d2, couldNotRevive: p2 };
  }, l2 = (a3, d2 = y.LOCAL) => {
    const p2 = Q2(a3, e2, t2.selectorReviveFn), g2 = P2(p2.target.selector);
    return g2 && o2.addAnnotation(p2, d2), g2;
  }, y3 = (a3, d2 = y.LOCAL) => {
    const { revived: p2, couldNotRevive: g2 } = c2(a3);
    return o2.syncAnnotations(p2, d2), g2;
  }, m2 = (a3, d2 = true, p2 = y.LOCAL) => d2 ? y3(a3, p2) : h2(a3, p2), E = (a3, d2, p2) => {
    if (typeof a3 == "string") {
      const g2 = Q2(d2, e2, t2.selectorReviveFn);
      o2.updateAnnotation(a3, g2, p2);
    } else {
      const g2 = Q2(a3, e2, t2.selectorReviveFn);
      o2.updateAnnotation(g2, d2);
    }
  }, h2 = (a3, d2 = y.LOCAL) => {
    const { revived: p2, couldNotRevive: g2 } = c2(a3);
    return o2.bulkUpsertAnnotations(p2, d2), g2;
  }, A = (a3, d2 = y.LOCAL) => {
    const p2 = ee2(a3, e2, t2.selectorReviveFn);
    o2.updateTarget(p2, d2);
  }, u2 = (a3, d2 = y.LOCAL) => {
    const p2 = a3.map((g2) => ee2(g2, e2, t2.selectorReviveFn));
    o2.bulkUpdateTargets(p2, d2);
  };
  function R(a3, d2, p2, g2) {
    const C = p2 || !!g2, S = i2.getAt(a3, d2, C).map((L) => o2.getAnnotation(L)).filter(Boolean), b2 = g2 ? S.filter(g2) : S;
    if (b2.length !== 0)
      return p2 ? b2 : b2[0];
  }
  const w2 = (a3) => i2.getAnnotationRects(a3).length > 0 ? i2.getAnnotationBounds(a3) : void 0, T = (a3, d2, p2, g2) => i2.getIntersecting(a3, d2, p2, g2), N3 = (a3) => i2.getAnnotationRects(a3), x2 = () => i2.recalculate(), _2 = (a3) => i2.on("recalculate", a3);
  return o2.observe(({ changes: a3 }) => {
    const d2 = (a3.deleted || []).filter((C) => P2(C.target.selector)), p2 = (a3.created || []).filter((C) => P2(C.target.selector)), g2 = (a3.updated || []).filter((C) => P2(C.newValue.target.selector));
    d2?.length > 0 && d2.forEach((C) => i2.remove(C.target)), p2.length > 0 && i2.set(p2.map((C) => C.target), false), g2?.length > 0 && g2.forEach(({ newValue: C }) => i2.update(C.target));
  }), {
    store: {
      ...o2,
      addAnnotation: l2,
      bulkAddAnnotations: m2,
      bulkUpdateTargets: u2,
      bulkUpsertAnnotations: h2,
      getAnnotationBounds: w2,
      getAnnotationRects: N3,
      getIntersecting: T,
      getAt: R,
      recalculatePositions: x2,
      onRecalculatePositions: _2,
      syncAnnotations: y3,
      updateAnnotation: E,
      updateTarget: A
    },
    selection: s2,
    hover: n2,
    viewport: r2
  };
};
var ce2 = 300;
var Ee2 = ["up", "down", "left", "right"];
var be2 = fe2 ? "⌘+a" : "ctrl+a";
var ut = [
  ...Ee2.map((e2) => `shift+${e2}`),
  be2
];
var ft = (e2, t2, o2, i2) => {
  const { store: s2, selection: n2 } = t2;
  let r2;
  const {
    annotatingEnabled: c2,
    offsetReferenceSelector: l2,
    selectionMode: y3,
    dismissOnNotAnnotatable: m2 = "NEVER"
  } = i2;
  let E, h2 = c2, A = "CREATE_NEW", u2, R, w2, T;
  const N3 = (f3) => {
    h2 = f3, g2.clear(), f3 || (R = void 0, u2 = void 0, w2 = void 0, T = void 0);
  }, x2 = (f3) => A = f3 || "CREATE_NEW", _2 = (f3) => E = f3, a3 = (f3) => r2 = f3, d2 = (f3) => {
    if (A === "ADD_TO_CURRENT")
      return true;
    if (i2.allowModifierSelect) {
      const v2 = f3;
      return fe2 ? v2.metaKey : v2.ctrlKey;
    } else
      return false;
  }, p2 = () => {
    if (!h2 || w2 === false) return;
    const { selected: f3 } = n2;
    if ((d2(T) || A === "REPLACE_CURRENT") && f3.length === 1 && f3[0].editable) {
      const k = s2.getAnnotation(f3[0].id);
      if (k?.target) {
        R = k.target, u2 = {
          annotation: k.id,
          selector: [],
          created: R.created,
          creator: R.creator,
          updated: /* @__PURE__ */ new Date(),
          updatedBy: r2
        };
        return;
      }
    }
    R = void 0, u2 = {
      annotation: v4_default(),
      selector: [],
      created: /* @__PURE__ */ new Date(),
      creator: r2
    };
  }, g2 = debounce((f3) => {
    if (!h2) return;
    const v2 = document.getSelection();
    if (!v2?.anchorNode) return;
    const I2 = W2().map((O) => We(O, e2));
    if (I2.every((O) => !me2(e2, O))) {
      u2 = void 0;
      return;
    }
    const D = f3.timeStamp - (T?.timeStamp || f3.timeStamp);
    if (T?.type === "pointerdown" && (D < 1e3 && !u2 || v2.isCollapsed && D < ce2) && p2(), !u2 && (p2(), !u2))
      return;
    if (v2.isCollapsed) {
      s2.getAnnotation(u2.annotation) && !(d2(T) || A === "REPLACE_CURRENT") && (n2.clear(), s2.deleteAnnotation(u2.annotation));
      return;
    }
    if (I2.every((O) => He2(O))) return;
    const B2 = I2.flatMap((O) => Ke(e2, O.cloneRange()));
    if (!(B2.length > 0 && !u2 || B2.length !== u2.selector.length || B2.some((O, Y) => O.toString() !== u2?.selector[Y]?.quote))) return;
    const j2 = d2(T) && R ? Pe2([
      ...R.selector.map((O) => O.range),
      ...B2
    ]) : B2;
    u2 = {
      ...u2,
      selector: j2.map((O) => Ye2(O, e2, l2)),
      updated: /* @__PURE__ */ new Date()
    }, !(d2(T) || A === "REPLACE_CURRENT") && (s2.getAnnotation(u2.annotation) ? s2.updateTarget(u2, y.LOCAL) : n2.clear());
  }, 10), C = (f3) => {
    T = te2(f3), w2 = T.button === 0;
  }, S = async (f3) => {
    if (!w2) return;
    const v2 = te2(f3), k = () => {
      const { x: D, y: B2 } = e2.getBoundingClientRect();
      if (J2(e2, v2.target)) {
        (typeof m2 == "function" ? m2(v2, e2) : m2 === "ALWAYS") && n2.clear();
        return;
      }
      const H2 = v2.target instanceof Node && e2.contains(v2.target) && s2.getAt(
        v2.clientX - D,
        v2.clientY - B2,
        y3 === "all",
        E
      );
      if (H2) {
        const { selected: j2 } = n2, O = new Set(j2.map((X) => X.id)), Y = Array.isArray(H2) ? H2.map((X) => X.id) : [H2.id];
        (O.size !== Y.length || !Y.every((X) => O.has(X))) && (o2.emit("clickAnnotation", H2), n2.userSelect(Y, v2));
      } else
        n2.clear();
    };
    if (v2.timeStamp - (T?.timeStamp || 0) < ce2) {
      await b2();
      const D = document.getSelection(), B2 = J2(e2, T?.target), H2 = J2(e2, v2.target);
      if (D?.isCollapsed || B2 && H2) {
        u2 = void 0, k();
        return;
      }
    }
    u2 && u2.selector.length > 0 && ($3(), n2.userSelect(u2.annotation, v2));
  }, b2 = async () => {
    const f3 = document.getSelection();
    let v2 = false, k = f3?.isCollapsed;
    const I2 = () => k || v2, D = 1;
    return setTimeout(() => v2 = true, 50), a2(() => k = f3?.isCollapsed, D, I2);
  }, L = (f3) => {
    document.getSelection()?.isCollapsed || ((!u2 || u2.selector.length === 0) && g2(f3), u2 && ($3(), n2.userSelect(u2.annotation, te2(f3))));
  }, M3 = (f3) => {
    if (h2 && !f3.repeat && W2().length !== 0 && f3.key === "Shift" && u2) {
      const v2 = document.getSelection();
      v2 && !v2.isCollapsed && ($3(), n2.userSelect(u2.annotation, Z(f3)));
    }
  }, V2 = (f3) => {
    const v2 = () => setTimeout(() => {
      u2 && (u2.selector.length > 0 && (n2.clear(), s2.addAnnotation({
        id: u2.annotation,
        bodies: [],
        target: u2
      }), n2.userSelect(u2.annotation, Z(f3))), document.removeEventListener("selectionchange", v2));
    }, 100);
    document.addEventListener("selectionchange", v2), p2();
  };
  hotkeys(ut.join(","), { element: e2, keydown: true, keyup: false }, (f3) => {
    f3.repeat || (T = Z(f3));
  }), hotkeys(be2, { keydown: true, keyup: false }, (f3) => {
    T = Z(f3), V2(f3);
  });
  const z2 = (f3) => {
    f3.repeat || W2().length !== 0 && (u2 = void 0, n2.clear());
  };
  hotkeys(Ee2.join(","), { keydown: true, keyup: false }, z2), document.addEventListener("pointerdown", C), document.addEventListener("pointerup", S), document.addEventListener("contextmenu", L), document.addEventListener("keyup", M3), e2.addEventListener("selectstart", p2), document.addEventListener("selectionchange", g2);
  const q2 = () => {
    u2 = void 0, R = void 0, w2 = void 0, T = void 0, g2.clear(), document.removeEventListener("pointerdown", C), document.removeEventListener("pointerup", S), document.removeEventListener("contextmenu", L), document.removeEventListener("keyup", M3), e2.removeEventListener("selectstart", p2), document.removeEventListener("selectionchange", g2), hotkeys.unbind();
  }, W2 = () => {
    const f3 = document.getSelection();
    return f3 ? Array.from(Array(f3.rangeCount).keys()).map((v2) => f3.getRangeAt(v2)).filter((v2) => v2.intersectsNode(e2)) : [];
  }, $3 = () => {
    if (!u2) return;
    const f3 = s2.getAnnotation(u2.annotation);
    if (!f3)
      s2.addAnnotation({
        id: u2.annotation,
        bodies: [],
        target: u2
      });
    else {
      const { target: { updated: v2 } } = f3, { updated: k } = u2;
      (!v2 || !k || v2 < k) && s2.updateTarget(u2);
    }
  };
  return {
    destroy: q2,
    setFilter: _2,
    setUser: a3,
    setAnnotatingEnabled: N3,
    setAnnotatingMode: x2
  };
};
var gt = (e2) => [
  `background-color:${w(e2?.fill || U.fill).alpha(e2?.fillOpacity === void 0 ? U.fillOpacity : e2.fillOpacity).toHex()}`,
  e2?.underlineThickness ? "text-decoration:underline" : void 0,
  e2?.underlineColor ? `text-decoration-color:${e2.underlineColor}` : void 0,
  e2?.underlineOffset ? `text-underline-offset:${e2.underlineOffset}px` : void 0,
  e2?.underlineThickness ? `text-decoration-thickness:${e2.underlineThickness}px` : void 0
].filter(Boolean).join(";");
var mt = () => {
  const e2 = document.createElement("style");
  document.getElementsByTagName("head")[0].appendChild(e2);
  let t2 = /* @__PURE__ */ new Set();
  return {
    destroy: () => {
      CSS.highlights.clear(), e2.remove();
    },
    setVisible: (n2) => {
      console.log("setVisible not implemented on CSS Custom Highlights renderer");
    },
    redraw: (n2, r2, c2, l2, y3) => {
      const m2 = new Set(n2.map((h2) => h2.annotation.id));
      Array.from(t2).filter((h2) => !m2.has(h2));
      const E = n2.map((h2) => {
        c2 ? typeof c2 == "function" && c2(h2.annotation, h2.state) : h2.state?.selected;
        const A = Se(h2, c2);
        return `::highlight(_${h2.annotation.id}) { ${gt(A)} }`;
      });
      e2.innerHTML = E.join(`
`), CSS.highlights.clear(), n2.forEach(({ annotation: h2 }) => {
        const A = h2.target.selector.map((R) => R.range), u2 = new Highlight(...A);
        CSS.highlights.set(`_${h2.id}`, u2);
      }), t2 = m2;
    }
  };
};
var pt = (e2, t2, o2) => ye(mt(), e2, t2, o2);
var ht = (e2, t2) => ({
  ...e2,
  annotatingEnabled: e2.annotatingEnabled ?? t2.annotatingEnabled,
  user: e2.user || t2.user
});
var le2 = "SPANS";
var _t = (e2, t2 = {}) => {
  Qe2(e2), Ue2(e2);
  const o2 = ht(t2, {
    annotatingEnabled: true,
    user: je()
  }), i2 = dt(e2, o2), { selection: s2, viewport: n2 } = i2, r2 = i2.store, c2 = ke(r2), l2 = Pe(i2, c2, o2.adapter);
  let y3 = o2.user;
  const m2 = typeof o2.renderer != "function" ? o2.renderer === "CSS_HIGHLIGHTS" ? CSS.highlights ? "CSS_HIGHLIGHTS" : le2 : o2.renderer || le2 : null, E = m2 === null ? o2.renderer(
    e2,
    i2,
    n2
  ) : m2 === "SPANS" ? it(
    e2,
    i2,
    n2
  ) : m2 === "CSS_HIGHLIGHTS" ? pt(
    e2,
    i2,
    n2
  ) : void 0;
  if (!E)
    throw `Unknown renderer implementation: ${o2.renderer}`;
  console.debug(m2 ? `Using ${m2} renderer` : "Using custom renderer implementation"), o2.style && E.setStyle(o2.style);
  const h2 = ft(
    e2,
    i2,
    l2,
    o2
  );
  h2.setUser(y3);
  const A = ze(i2, c2, o2.adapter), u2 = () => y3, R = (a3) => {
    h2.setAnnotatingEnabled(
      a3 === void 0 ? true : a3
    );
  }, w2 = (a3) => {
    h2.setAnnotatingMode(a3);
  }, T = (a3) => {
    E.setFilter(a3), h2.setFilter(a3);
  }, N3 = (a3) => {
    y3 = a3, h2.setUser(a3);
  }, x2 = (a3) => {
    a3 ? s2.setSelected(a3) : s2.clear();
  };
  return {
    ...A,
    destroy: () => {
      E.destroy(), h2.destroy(), c2.destroy();
    },
    element: e2,
    getUser: u2,
    renderer: E,
    setAnnotatingEnabled: R,
    setAnnotatingMode: w2,
    setFilter: T,
    setStyle: E.setStyle.bind(E),
    setUser: N3,
    setSelected: x2,
    setVisible: E.setVisible.bind(E),
    on: l2.on,
    off: l2.off,
    scrollIntoView: Ve(e2, r2),
    state: i2
  };
};
export {
  re2 as DEFAULT_SELECTED_STYLE,
  U as DEFAULT_STYLE,
  ge2 as NOT_ANNOTATABLE_CLASS,
  K2 as NOT_ANNOTATABLE_SELECTOR,
  y as Origin,
  ne as UserSelectAction,
  Ot as W3CTextFormat,
  Qe2 as cancelSingleClickEvents,
  Z as cloneKeyboardEvent,
  te2 as clonePointerEvent,
  Se as computeStyle,
  Je as computeZIndex,
  Ie as createBody,
  ye as createRenderer,
  ft as createSelectionHandler,
  it as createSpansRenderer,
  _t as createTextAnnotator,
  dt as createTextAnnotatorState,
  ne2 as debounce,
  ht as fillDefaults,
  Ze as getBackgroundColor,
  et as getHighlightClientRects,
  $e as getQuoteContext,
  se2 as getRangeAnnotatableContents,
  at as getViewportBounds,
  fe2 as isMac,
  Nt as isNodeWhitespaceOrEmpty,
  J2 as isNotAnnotatable,
  me2 as isRangeAnnotatable,
  He2 as isRangeWhitespaceOrEmpty,
  P2 as isRevived,
  xt as isRevivedAnnotation,
  Lt as isRevivedTarget,
  ot as mergeClientRects,
  Pe2 as mergeRanges,
  je2 as parseW3CTextAnnotation,
  Ue2 as programmaticallyFocusable,
  Ye2 as rangeToSelector,
  Q2 as reviveAnnotation,
  ee2 as reviveTarget,
  Xe as reviveTextSelector,
  Ve as scrollIntoView,
  Ge2 as serializeW3CTextAnnotation,
  Ke as splitAnnotatableRanges,
  kt as toDomRectList,
  st as toParentBounds,
  Mt as toViewportBounds,
  ct as trackViewport,
  We as trimRangeToContainer,
  pe2 as whitespaceOrEmptyRegex
};
//# sourceMappingURL=@recogito_text-annotator.js.map
